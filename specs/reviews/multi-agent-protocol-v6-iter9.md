---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після 4 фіксів iter8)
iter: 9
agent: Opus subagent (чистий контекст; артефакти iter1–8; 5 клонів у шляху з пробілом, autocrlf=true, --single-branch; дві спеки — ## і ##/### з групами)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md … iter8.md
decision: FAIL → 1 дискваліфікуюча + 1 нотатка; обидві виправлено; iter10 дозволена наперед (остання з трьох)
---

# Контекст

4/4 фіксів iter8 підтверджено як приземлені й ефективні: G0 зсередини linked worktree
(основний клон на `main` і detached) — ff основного клона, worktree лишається на task-гілці;
§0 у клоні з stray-комітом → СТОП; awk на межі рівнів — `### TASK-102` не захоплює
`## Group B`, G7 для неї проходить після правки прози групи; «чотири критерії»;
`tr -d '\r'` спрацьовує на CRLF-checkout. Усе ядро — без знахідок. Дві нові — обидві
в екстракторі секції критеріїв.

# Знахідки та рішення

| # | Рядок | Цитата | Суть | Рішення |
|---|---|---|---|---|
| 1 (дискв.) | 333 | `f&&$0!=h&&match($0,/^#+ /)&&RLENGTH-1<=L{exit} f'` | `# коментар` усередині ``` fenced-блоку в секції критеріїв — це «заголовок» рівня 1 → копія обрізається мовчки, guard `[ -n ]` проходить, G7 обрізає так само → критерії нижче можна переписати без BLOCKED (відтворено) | **Виправлено.** awk трекає fence (`/^```/{b=!b}`) і ігнорує заголовки при `b`; додано `sub(/\r$/,"")` для POSIX awk на CRLF; обидва місця (5.2, G7) |
| 2 (нотатка) | 334 | `[ -n "$CRIT" ]   # секцію знайдено.` | `$0==h` вимагає заголовок ТОЧНО `## <ID>`; з назвою після ID — порожній `$CRIT` і мовчазний `exit 1` без жодного тексту | **Виправлено.** Guard з повідомленням; вимога записана в коментарі §5.2 і в `criteria_section` binding |

Перевірено (T22): fence із `# коментар` не обрізає; групова розкладка не зламалась;
CRLF-вхід дає ті самі 12 рядків; заголовок із назвою → гучний абор.

# Що підтверджено як коректне (iter9)

§5.2 (гонка, ранній абор + очистка зсередини worktree + повторний claim); §5.5 (той
самий, свіжий, `--single-branch`, після RECLAIM → СТОП); §5.6; G7 (i)/(ii) для обох
розкладок, включно з останньою `###`-секцією групи + правкою прози наступної групи;
§6.0 + G7-lite ×2 з видаленням; 5.4 vs INTEGRATED; уся внутрішня консистентність; §12;
§14 13/13; проєктна незалежність.

# Залишкова невизначеність

- Git for Windows 2.52, MSYS awk/sed/diff; хостинги і POSIX-інструменти не перевірялись
  (тепер awk має `sub(/\r$/,"")`, але наживо під POSIX awk не запускали).
- G2–G6, G8, §7, §8, §11, §13, §15 — проза, перевірена на консистентність.
