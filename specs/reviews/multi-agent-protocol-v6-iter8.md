---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після 4 фіксів iter7)
iter: 8
agent: Opus subagent (чистий контекст; артефакти iter1–7; клони в шляху з пробілом, autocrlf=true, --single-branch, спека з ### і групами ##)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md … iter7.md
decision: FAIL → 3 дискваліфікуючі + 1 нотатка; усі 4 виправлено; iter9 дозволена наперед (людина: «до PASS або до 3 ітерацій»)
---

# Контекст

4/4 фіксів iter7 підтверджено як приземлені (ff+SHA у §0 працює в основному клоні;
четвертий критерій 6.0; awk із рівнем; видалення `small/*`). Три з них породили нові
дефекти на межах: ff-крок зсередини linked worktree, awk на межі рівнів, лічильник
критеріїв у G7-lite. Ядро §5.2/5.5/5.6, G7 для `##`-спек, 5.4 — без знахідок.

# Знахідки та рішення

| # | Рядок | Цитата | Суть | Рішення |
|---|---|---|---|---|
| 1 (дискв.) | 620 | `git switch -q main && git merge -q --ff-only origin/main \` | G0 виконується після claim — зсередини worktree; `switch main` там падає («main зайнятий») → хибний СТОП, або (якщо основний клон detached) переносить worktree на `main` (відтворено) | **Виправлено.** `MAIN=$(git rev-parse --git-common-dir)/..` і всі `switch`/`merge`/`rev-parse` через `git -C "$MAIN"` у §0 і G0; примітка, що свіжість worktree — §5.6 |
| 2 (дискв.) | 347 | «заголовку ТОГО Ж рівня, а не на будь-якому "## "» | Остання `###`-секція групи захоплює наступний `## Group` і його прозу → чужа правка блокує одиницю (відтворено) | **Виправлено.** awk зупиняється на заголовку того ж АБО вищого рівня (`match(/^#+ /)`, `RLENGTH-1<=L`), глибші — частина секції; обидва місця (5.2, G7) |
| 3 (дискв.) | 601 | «перевіряє три критерії 6.0 по diff'у» | Критеріїв чотири з iter7; master не перевіряє саме новий | **Виправлено.** «ВСІ чотири критерії 6.0» |
| 4 (нотатка) | 508 | `OWN=$(sed -n 's/^OWNER=//p' …)` | CRLF-checkout під POSIX sed → `agent-foo\r` ≠ `agent-foo` → законний власник СТОП | **Виправлено.** `\| tr -d '\r'` |

Перевірено (T21): `### TASK-102` (остання в групі) зупиняється на `## Group B`, зберігає
`#### deeper`; `## Group A` включає свої `###`; G0-крок зсередини worktree ff-ить основний
клон, worktree лишається на `task/TASK-001`.

# Що підтверджено як коректне (iter8)

§5.2 (гонка, `AGENT_ID` unset, ранній абор + очистка зсередини + повторний claim); §5.5
(той самий клон, свіжий, `--single-branch` в окремій теці, після RECLAIM → СТОП); §5.6;
G7 (i)/(ii) для `##` і для нетермінальних `###`; G7-lite ×2 з видаленням гілки, конфлікт
двох `small/*` — звичайний rebase-конфлікт; 5.4 vs INTEGRATED — відмова; уся внутрішня
консистентність; §12; §14 13/13; проєктна незалежність.

# Залишкова невизначеність

- Локальні bare-репо, Git for Windows, MSYS-інструменти; хостинги й POSIX sed/awk — ні.
- G2–G6, G8, §7, §8, §11, §13, §15 — проза, перевірена на консистентність.
