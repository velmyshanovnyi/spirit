---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після 10 фіксів iter3; ітерація понад ліміт, дозволена людиною)
iter: 4
agent: Opus subagent (чистий контекст; на вхід — файл, критерії, артефакти iter1–3)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md
  - specs/reviews/multi-agent-protocol-v6-iter2.md
  - specs/reviews/multi-agent-protocol-v6-iter3.md
decision: FAIL → 7 нових знахідок (4 дискваліфікуючі); ліміт знову вичерпано → HALT оновлено
---

# Контекст

Рев'юер перевірив 10 фіксів iter3: **10/10 приземлились і ефективні** для заявленої
мети (усі рецепти виконано зі скриптів у bare-репо: гонка, `AGENT_ID` unset, поганий
шлях критеріїв, очистка зсередини worktree, три сценарії §5.5, §5.6, цикл G0, G7
CRITERIA, ім'я `small/*`). Два з них (#1, #5) при цьому відкрили нові дефекти в сусідніх
місцях. 7 нових знахідок, жодна не повтор. Цитати звірено. **Жодну не дисміснуто; жодну
не виправлено** — дозволена людиною додаткова ітерація використана.

# Знахідки (не виправлені)

Дискваліфікуючі: 1–4. Нотатки: 5–7 + два спостереження.

| # | Рядок | Цитата | Суть | Запланований фікс |
|---|---|---|---|---|
| 1 | 474 | «або чужа робота після RECLAIM (5.4). Обидва випадки — умова СТОП.» | Guard §5.5 перевіряє лише розходження; RECLAIM — звичайний коміт у ту саму гілку → колишній власник ff-иться в чужу одиницю мовчки (відтворено) | Після `merge --ff-only` порівняти `OWNER=` у handoff з `$AGENT_ID`; не збігається → СТОП |
| 2 | 459 | `WT=$(git worktree list --porcelain \| awk '/^worktree /{p=$2} …` | `$2` обрізає шлях на першому пробілі — на Windows це типово; §5.2↔§5.5 стають замкненим колом без виходу (відтворено) | `p=substr($0,10)` |
| 3 | 310 | `\|\| { echo "локальна task/TASK-001 вже існує — спершу 5.5"; exit 1; }` | Абор до push (поганий шлях критеріїв) лишає worktree+гілку; очистка CLAIM_LOST описана лише для `exit != 0` на push; вказівник на §5.5 веде до хибного «розійшлася з origin» для ref, якого на сервері нема (відтворено) | Очистку описати як «після будь-якого збою рецепта»; у §5.5 guard спершу перевіряти існування `origin/task/<ID>`, інакше «claim не відбувся — очисти й повтори 5.2» |
| 4 | 716 | «кожен шлях@blob із CRITERIA handoff'у досі дорівнює `git rev-parse origin/main:<шлях>`» | **Дизайн.** Blob цілого файлу спеки змінюється при G9 будь-якої сусідньої секції → усі інші одиниці цієї спеки блокуються (відтворено: 2 агенти, 2 секції, після інтеграції першої друга BLOCKED) | Варіанти: (а) копіювати текст критеріїв приймання у handoff при claim — handoff і є замороженими критеріями, G7 порівнює текст секції; (б) хеш тексту секції, виділеної за заголовком; (в) прибрати CRITERIA, лишити «критерії — це стан на BASE_SHA, зміни після — G1 повторно». Потребує рішення людини |
| 5 | 1097 | `будь-який → BLOCKED   умова СТОП (розділ 9); назад — у той самий стан або в ABANDONED` | У 6.10 немає рядка на вихід із BLOCKED → worker може сам розблокувати одиницю з `DEPLOY=PARTIAL` | Рядок «вихід із BLOCKED» ❌ ✅ (лише master / людина) |
| 6 | 997 | «виконує master, і він не може бути автором зміни.» | §8.2 забороняє master інтегрувати власну роботу; 6.10 дозволяє master claim; §2.1 каже слабше; binding не вимагає ≥2 master → одиничний master або порушує §8.2, або вічний REVIEW | Явне правило: master, що є автором, потребує другого master для G7; якщо його нема — умова СТОП + binding `roles.master` мінімум 2 при `master` як worker |
| 7 | 193 | «зміна захищених шляхів поза межами своєї одиниці роботи» | Неявний дозвіл на in-scope зміни protected_paths суперечить 6.10 «зміна архітектурних специфікацій ❌»; G9 вимагає тікати чекбокси в спеці, яка може бути protected | Розділити: чекбокси/handoff у protected спеці — дозволено worker як частина G9; зміна нормативного тексту — лише master |
| н1 | 553 | G7-lite «зливає fast-forward» | Друга `small/*` гілка від тієї ж бази ff не зіллється; кроку rebase нема | Додати rebase у G7-lite |
| н2 | 565 | цикл G0 по `refs/remotes/origin/task/*` | `--single-branch` клон їх не має → G0 мовчки каже «нічого в роботі», а `ls-remote` показує claim | У §0/G0: `git fetch origin '+refs/heads/task/*:refs/remotes/origin/task/*'` явно |

# Після halt #2: людина обрала варіант (а) для #4 і дозволила iter5 (2026-09-06)

Застосовано всі 7 фіксів + 2 спостереження. #4: `CRITERIA_SOURCE=<файл#заголовок>` і
дослівна копія секції в handoff після `---`; G7 порівнює текст секції на `origin/main`
з копією після нормалізації `[x]`→`[ ]` (рецепт у G7). Перевірено (T15): дві одиниці
однієї спеки, claim обох; G9-тіки першої не блокують другу; реальна зміна критеріїв
другої — виявляється. Також: `OWNER=` ≠ `AGENT_ID` після RECLAIM → СТОП; шлях клона з
пробілом працює (`substr`); ранній абор → та сама очистка. Далі — iter5.

# Що підтверджено як коректне (iter4)

Арбітраж claim за рецептом; попередження про порожню гілку; `--force`; §5.6 повністю;
факти про `pull --ff-only`, `worktree remove` зсередини, `:` у refname; цикл G0 і
`ls specs/handoffs` на `main`; механіка G7 CRITERIA (дефект — у сфері дії, не в
механіці); покриття §10 ↔ 6.10 (крім виходу з BLOCKED); усі ключі §12; §14 13/13;
проєктна незалежність; §7, §8.2, §8.3, §11 (16 антипатернів), §13, §15, §16.

# Залишкова невизначеність

- Локальні bare-репо, Git for Windows 2.52, bash; хостинги не перевірялись.
- 5–7 — конфлікти правил, підтверджені читанням, не виконанням.
- Пробіл у шляху перевірено; таби/переноси/`core.worktree` — ні.
