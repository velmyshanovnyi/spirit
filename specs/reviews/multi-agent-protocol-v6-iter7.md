---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після 4 фіксів iter6)
iter: 7
agent: Opus subagent (чистий контекст; артефакти iter1–6; 6 клонів у шляху з пробілом, autocrlf=true, --single-branch в окремій теці)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md … iter6.md
decision: FAIL → 1 дискваліфікуюча + 3 нотатки; усі 4 виправлено; дозволені людиною 2 ітерації використано → HALT
---

# Контекст

5/5 фіксів iter6 підтверджено (зокрема §5.5 у `--single-branch` без upstream + явний
push; `small/*` видно; 5.4 відмовляє INTEGRATED; CRLF-checkout проходить G7). Уперше
ані ядро §5.2/5.5/5.6, ані G7, ані §6.0 не дали жодної знахідки — усі 4 нові стосуються
розвідки (§0/G0) та крайових конвенцій.

# Знахідки та рішення

| # | Рядок | Цитата | Суть | Рішення |
|---|---|---|---|---|
| 1 (дискв.) | 30 | `grep -rn '\- \[ \]' specs/ --exclude-dir=handoffs` | §0/G0 роблять лише `fetch`; `ls specs/reviews/` і `grep` читають робоче дерево, яке ніхто не оновлює → застарілий список одиниць і відсутня історія рев'ю (відтворено: `[behind 5]`, теки `specs/reviews/` «нема») | **Виправлено.** Після fetch: `git switch main && git merge --ff-only origin/main` (claim main не чіпає → ff завжди можливий; не ff → СТОП) у §0 і G0 |
| 2 (нотатка) | 568 | «не змінює спостережуваної поведінки: документація, коментар, typo» | Typo-фікс у секції критеріїв заявленої одиниці → G7 чужої одиниці BLOCKED | **Виправлено.** Четвертий критерій 6.0: не торкатись секцій критеріїв заявлених одиниць |
| 3 (нотатка) | 324 | `awk … f&&/^## /&&$0!=h{exit} f'` | Термінатор `## ` захардкоджений, а `criteria_section` конфігурований → для `###` секцій витяг захоплює сусідів | **Виправлено.** awk бере рівень із `h`: `l=substr(h,1,index(h," "))`, кінець — наступний заголовок того ж рівня (обидва місця) |
| 4 (нотатка) | 610 | `refs/remotes/origin/small/ # master: черга G7-lite` | `small/*` після злиття не видаляються → черга не порожніє | **Виправлено.** G7-lite завершується `git push origin --delete small/<name>` |

# Що підтверджено як коректне (iter7)

§5.2 (гонка, ранній абор, очистка зсередини worktree, повторний claim); §5.5 (той самий
клон із пробілом, свіжий звичайний, свіжий `--single-branch` в окремій теці, після
RECLAIM → СТОП, явний push з відновленого worktree); §5.6; §0/G0 у `--single-branch`
клоні (`origin/main` рухається, `task/*` і `small/*` видно, повторний голий
`fetch --prune` не вирізає їх); G7 (i)/(ii) включно з rebase на 3 коміти; G7-lite для
двох `small/*`; 5.4 проти INTEGRATED — відмова; уся внутрішня консистентність; §12
ключі; §14 13/13; проєктна незалежність.

# Залишкова невизначеність

- Локальні bare-репо, Git for Windows; хостинги не перевірялись (як і в iter1–6).
- POSIX sed/awk/diff поза MSYS не перевірялись.
- G2–G6, G8, §7, §8, §11, §13, §15 — проза, перевірена лише на консистентність.
