---
spec: specs/phase5/test-fixture-fidelity.md
section: X1+X2 — генерована фікстура з index.html + real-trigger тест розблокування
iter: 1
agent: Opus subagent (чистий контекст; git diff; перевіряв межі зрізу, drift-guard у обидва боки, гілки user-intent у router.js)
files-reviewed:
  - client/tests/app.test.js
  - client/index.html
  - specs/phase5/test-fixture-fidelity.md
decision: PASS_WITH_NOTES → 2 нотатки, обидві виправлено
---

# Нотатки та рішення

| # | Місце | Суть | Рішення |
|---|---|---|---|
| 1 | app.test.js (fixture IIFE) | `indexOf("<body>")` при майбутньому `<body class="...">` поверне −1 і мовчки вклеїть `<head>` у фікстуру; drift-guard цього не зловить | **Виправлено** — явний `throw` при відсутності простого `<body>`-тега |
| 2 | drift-guard | `realIds` сканував ВЕСЬ файл, а фікстура — body-мінус-скрипти: id у `<head>` дав би хибний drift-фейл | **Виправлено** — guard порівнює проти `INDEX_BODY` (той самий зріз) |

# Що рев'юер підтвердив як коректне

1. Межі зрізу: head-скрипт (рядок 29) поза зрізом; єдиний in-body `<script>`
   (bootstrap, 770) вирізається non-greedy regex-ом; літерального `</script>`
   у рядках/атрибутах index.html немає — обрізання контенту неможливе;
   TEST_ONLY_MARKUP — рівно один елемент (#btn-generate).
2. Drift-guard працює в обидва боки: додавання у фікстуру без прод-HTML — фейл;
   застарілий запис у TEST_ONLY_IDS — теж фейл (асертиться і присутність у
   фікстурі, і ВІДСУТНІСТЬ у проді).
3. `id="account-heading"`: без колізій (uniq -d порожній), нуль референсів у
   js/css — впливу на прод немає.
4. Заміни літералів: дослівно збігаються з value-атрибутами index.html
   (423, 437; google-client-id без value → порожній); єдині вцілілі
   `node.example` — тест, що сам задає поле (правильно не зачеплений).
5. **X2 змістовний у обидва боки**: router.js:207 (`isTrusted === true`)
   робить синтетичний hashchange програмним (модалка лишається прихованою),
   а router.js:175 ставить `userInitiated: true` з click-СЛУХАЧА, не з
   isTrusted — тож jsdom-клік легітимно потрапляє в user-гілку; регрес
   будь-якої гілки валить тест.
6. Борг заявлено чесно: #btn-generate («видимий борг», ~100 тестів) — у
   спеці й у закритті беклогу нижче.
7. Прогін рев'юера 400/400; повний прогін автора 1051/1051.

# Жива перевірка (обидва хости, 2026-09-19)

- index.html задеплоєно (єдина зміна — id="account-heading"); сторінка
  вантажиться, елемент присутній, консоль чиста.
