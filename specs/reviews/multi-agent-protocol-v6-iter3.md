---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після виправлень iter2)
iter: 3
agent: Opus subagent (чистий контекст; на вхід — файл, критерії приймання, артефакти iter1+iter2)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md
  - specs/reviews/multi-agent-protocol-v6-iter2.md
decision: FAIL → 10 нових знахідок; ліміт 3 ітерацій вичерпано → HALT (specs/multi-agent/MULTI-AGENT.halt.md)
---

# Контекст

Рев'юер виконав усі рецепти дослівно в bare-репо з 4 клонами (Git 2.52), перевірив
13 виправлень iter2: 9 приземлились і ефективні, 4 — приземлились, але недостатні
(#1 частково, #4 не ефективне, #7 не виконуване, #13 не примусове). 10 нових знахідок,
жодна не повторює iter1/iter2. Цитати звірено — збігаються. **Жодну не дисміснуто**;
жодну не виправлено, бо це була третя ітерація і правило 7.4 самого протоколу
(і гейт 3 `CLAUDE.md`) вимагає зупинки, а не четвертого кола без рішення людини.

# Знахідки (не виправлені — очікують рішення людини)

Дискваліфікуючі за оцінкою рев'юера: 1, 2, 3, 4, 5, 6. Нотатки: 7, 8, 9, 10.

| # | Рядок | Цитата | Суть | Запланований фікс (не застосований) |
|---|---|---|---|---|
| 1 | 454 | `git worktree list --porcelain` | §5.5 без `set -e`; `worktree add` при наявній теці `../wt-<ID>` створює гілку, падає на теці, блок завершується `exit 0` → агент працює в `main`. Також не описано найпоширеніший випадок: worktree уже існує з §5.2 | `set -e`; гілка «worktree уже є → `cd` в нього + `merge --ff-only`»; при чужій теці — СТОП |
| 2 | 499 | «Пропуск воріт на повному шляху дозволений лише з» | Загальний дозвіл пропускати будь-які ворота через `SKIPPED=` суперечить G2 «жорсткі», G7, §8.1, §9, §11 | Позначити G0, G2, G4, G6, G7 непропускними; `SKIPPED=` лише для G5 (незастосовно) і G8 (DEPLOY=NOT_APPLICABLE) |
| 3 | 748 | «залишають `STATE=INTEGRATED`, а не `DONE`.» | G8 лишає INTEGRATED при PARTIAL/UNKNOWN, а §10 каже «будь-який → BLOCKED» для умов СТОП → §5.4 може RECLAIM'нути одиницю з поламаним деплоєм | Єдине правило: PARTIAL/UNKNOWN → STATE=BLOCKED з `NOTE=integrated, deploy partial`; §5.4 не чіпає BLOCKED |
| 4 | 1065 | `REVIEW → APPROVED     master закриває review, що зійшовся` | Немає переходу REVIEW → ACTIVE після `decision: FAIL` | Додати `REVIEW → ACTIVE  decision FAIL, автор допрацьовує (iter < 3)` у §10 і рядок у 6.10 |
| 5 | 303 | `set -e  # будь-який збій нижче — claim НЕ робиться` | `set -e` не ловить збій `$(git rev-parse HEAD:<шлях>)` усередині heredoc → claim із `CRITERIA=…@HEAD:specs/missing.md` пушиться з `exit 0` і назавжди неінтегрований | Обчислити blob окремим рядком до heredoc: `CRIT_BLOB=$(git rev-parse --verify HEAD:specs/foo.md)`; те саме для `AGENT_ID` (#10) |
| 6 | 529 | `false → у гілку small/<UTC>-<slug>` | `date -u +%FT%TZ` дає `:` — заборонений символ у refname (`exit 128`) | Формат `small/$(date -u +%Y%m%dT%H%M%SZ)-<slug>` |
| 7 | 338 | `cd "$REPO"  # зсередини worktree його не видалити` | Після аварійного виходу з `set -e` це нова оболонка, `$REPO` порожній, `cd ""` = no-op → та сама помилка, що в iter2 #4 | `cd "$(git rev-parse --git-common-dir)/.."` замість `$REPO` |
| 8 | 782 | `INTEGRATED → DONE (G9, після записаного DEPLOY) ✅ ✅` | Після злиття handoff-файл уже в гілці інтеграції; worker не має права комітити туди → термінальний стан не записується | INTEGRATED → DONE записує **master** разом із G7/G8 (рядок 6.10 → ❌ ✅), або окремою гілкою `handoff/<ID>` + G7-lite |
| 9 | 184 | «Чого worker не робить без окремого дозволу в межах поточної сесії:» | Створює другий, незадекларований виняток із 6.10 (merge чужої роботи, deploy) | Прибрати кваліфікатор: «Чого worker не робить, крапка» + «дозвіл людини = зміна `roles` у binding, не фраза в чаті» |
| 10 | 126 | «визначити свій `AGENT_ID`, не заявляє роботу: умова СТОП.» | Рецепт §5.2 не перевіряє `AGENT_ID`; порожній → `OWNER=` пушиться з `exit 0` | `: "${AGENT_ID:?AGENT_ID не задано — СТОП (2.0)}"` на початку рецепта; пункт у §9 |

# Що рев'юер підтвердив як коректне (iter3)

Арбітраж claim за рецептом (переможець 0, той, що програв, `! [rejected]` 1, `main`
незмінний); попередження про порожню гілку точне; обмеження `--force` точне; §5.6
повністю; §5.5 у свіжому клоні й guard на розходження (СТОП, `exit 1`); блок G7 і
виявлення дрейфу CRITERIA; §2/§2.0/§2.1 (крім #9); §7.1–7.5; §8.2 ↔ 6.10 ↔ §8.1;
усі 14 посилань §11; усі 13 пунктів §14 «Додано у v6»; проєктна незалежність; усі
згадані в прозі ключі binding визначені в §12 (два визначені, але не прив'язані до
правил — `units_of_work.id_format`, `commands.freshness_check` — без наслідку, не
знахідки).

# Після halt: ліміт знято людиною на одну ітерацію (2026-09-06)

Людина обрала варіант 1 halt-артефакта. Усі 10 фіксів із колонки «Запланований фікс»
застосовано без змін дизайну; перевірено в bare-репо (T13a–h): порожній `AGENT_ID` і
неіснуючий шлях критеріїв зупиняють рецепт до push (0 ref на сервері), очистка CLAIM_LOST
через `--git-common-dir` працює зсередини worktree в новій оболонці, повторний вхід §5.5
працює в тому самому клоні (існуючий worktree), у свіжому клоні та зупиняється на чужій
теці, ім'я `small/<YYYYMMDDTHHMMSSZ>-<slug>` валідне. Далі — iter4.

# Залишкова невизначеність

- Усі експерименти — локальні bare-репо на Git for Windows; хостинги не перевірялись.
- #2, #3, #4, #8, #9 — конфлікти правил, не спостережувана поведінка.
- `cd ""` = успіх — bash-специфічно; під dash/zsh механізм #7 може відрізнятись.
