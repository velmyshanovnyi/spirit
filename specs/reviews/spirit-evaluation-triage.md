# Тріаж зовнішньої оцінки Spirit (`specs/reviews/spirit-evaluation.md`)

Джерело: незалежна оцінка проєкту від потужнішої моделі, стан на коміт
`01466f3` (24-25.07.2026), 13 агентів-читачів коду + жива перевірка на
обох хостах. Це не формальний крипто-аудит (документ сам це визнає, §11),
але методологія (жива перевірка запуском коду, агент-спростовувач на
знахідках високої важливості) достатньо сувора, щоб ставитись серйозно.

**Цей документ — лише тріаж і план, БЕЗ жодної зміни коду.** Мета:
розкласти кожне знайдене "доопрацювання" на дію (спека/бектрог) з чітким
статусом: підтверджено власною перевіркою / потребує рішення користувача
/ вже відомий свідомий компроміс / неточність самого звіту.

**Власна верифікація перед довірою**: перш ніж переносити знахідки в
бектрог, я перечитав код напряму для трьох найважчих тверджень (§A нижче)
замість сліпого копіювання. Два з трьох мій ПЕРШИЙ (subagent-делегований)
прохід помилково спростував — власне повторне читання коду показало, що
звіт мав рацію в усіх трьох випадках. Деталі — у примітках при кожному
пункті. Решта пунктів звіту перенесені без окремої повторної перевірки
кожного (обсяг завеликий для одного проходу) -- позначено відповідно.

---

## A. Критичні дефекти даних і довіри (перевірено власноруч, підтверджено)

Ці три — найважчі твердження звіту, і я свідомо перечитав код сам, а не
поклався на побіжну перевірку, бо перший делегований прохід їх помилково
спростував.

- [x] **A1. Портативний/детермінований вхід стирає локальну історію повідомлень при КОЖНОМУ вході, не лише при відновленні. ВИПРАВЛЕНО (коміт нижче).**
  Підтверджено читанням коду напряму: `client/js/app.js:4337` (`btn-login-portable`)
  безумовно викликає `adoptScalarIdentity` при КОЖНОМУ вході, яка веде в
  `profile.js:227` → `adoptIdentity` → `persistRawIdentity` (`profile.js:62-87`).
  Ця функція **завжди** генерує СВІЖУ сіль (`generateSalt()`, рядок 63) і
  явно видаляє (`remove("messages", key)`) усі рядки історії під
  `${profileId}:`-префіксом (рядки 79-84) — бо стара історія зашифрована
  під старим vault-ключем і стане недешифровуваною. Коментар у коді сам
  визнає механізм ("re-create/re-adopt of the same identity"), але не
  усвідомлює, що цей шлях виконується на КОЖНОМУ звичайному вході, а не
  тільки при повторному "adopt" сценарії. `hasStoredProfile()`
  (`profile.js:40`) — задокументований у коментарях як запобіжник, який
  мав би про це попереджати користувача — не викликається НІДЕ поза
  тестами (підтверджено грепом).
  Потребує рішення (не суто технічне): чи має детермінований/портативний
  вхід взагалі персистити vault-шифровану локальну історію під СВІЖОЮ
  сіллю щоразу, чи має перевикористовувати збережену сіль **для того
  самого профілю на тому самому пристрої** (як це вже робить звичайний
  розблокувальний шлях `loadPermanentProfile`) і генерувати нову лише
  коли профілю ще немає локально. Друге — очевидний фікс, але варто
  обговорити крайні випадки (портативний акаунт МАЄ намір заходити з
  різних пристроїв — на кожному НОВОМУ пристрої свіжа сіль правильна;
  проблема суто в РЕ-вході на ТОМУ САМОМУ пристрої). Вибрано другий
  варіант (перевикористовувати наявну сіль на тому самому пристрої),
  але не буквально "як `loadPermanentProfile`" -- через перевірку
  successful-decrypt, а не безумовно, щоб не зламати сценарій
  "справді інша парольна фраза → історія й так недешифровувана,
  wipe коректний" (уже покритий наявним тестом, лишився зеленим).
  - [x] **Tests**: `client/tests/profile.test.js`, новий тест у
        `describe("vault key and history isolation after restore/adopt")`:
        відтворює РЕАЛЬНИЙ портативний вхід (`adoptIdentity` з ТОЄЮ Ж
        парольною фразою вдруге, як робить `deriveAccountMaterial`+
        `adoptScalarIdentity` при кожному логіні), підтверджено RED до
        фіксу (`listMessages` повертав `[]` замість збереженого рядка).
        Наявний тест "re-adopting the SAME identity clears its
        now-undecryptable history rows" (РІЗНА парольна фраза) лишився
        зеленим без змін -- підтверджує, що фікс не зламав легітимний
        wipe-сценарій. 30/30 у `profile.test.js`, 311/311 у `app.test.js`
        (без регресій).
  - [x] **Impl**: `client/js/profile.js` -- `adoptIdentity` тепер
        спочатку перевіряє, чи вже є локальний запис `profile`-стору для
        цього `profileId`; якщо є -- виводить `vaultKey` з ЙОГО наявною
        сіллю і НАМАГАЄТЬСЯ розшифрувати наявний `encryptedPrivateKey`
        цим ключем. Успіх → повертає його БЕЗ виклику
        `persistRawIdentity` (історія не чіпається). Невдача (інша
        парольна фраза чи пошкоджений запис) АБО запис відсутній
        (перший вхід на цьому пристрої) → той самий шлях, що й раніше
        (`persistRawIdentity`, свіжа сіль, wipe undecryptable-історії).
  - [x] **Exec review**: самоперевірка -- (а) на першому вході на НОВОМУ
        пристрої (`existing === undefined`) поведінка побайтово незмінна
        (fallback-гілка); (б) на реальному портативному вході з тим самим
        паролем (`deriveAccountMaterial` дає той самий скаляр →
        `profileId` незмінний, `adoptScalarIdentity` передає ТОЙ САМИЙ
        `password` як `localPassphrase`) derive+decrypt гарантовано
        успішні -- математично, не ймовірнісно, оскільки обидва виведення
        йдуть з ідентичного `password`; (в) `hasStoredProfile()`
        лишається невикористаним поза тестами -- це ОКРЕМА,
        не термінова UX-прогалина (попередження перед перезаписом при
        СПРАВЖНЬОМУ re-adopt з іншим паролем), не змішана з цим фіксом;
        (г) `restoreProfileFromMnemonic`/`restoreFromKeyfile`/device-link
        grant-шлях (усі йдуть через той самий `adoptIdentity`) отримують
        той самий захист автоматично -- узгоджено з тим, що жоден з них
        не повинен мовчки стирати історію, якщо ідентичність і локальний
        пароль насправді не змінились; (д) 30/30 + 311/311, без регресій;
        (е) задеплоєно на обидва хости, живо підтверджено на
        `spirit.kibr.com.ua`: `createPermanentProfile` → додано рядок
        історії → `adoptIdentity` з ТИМ САМИМ паролем вдруге →
        `historyTexts` містить збережений рядок, `profileId` незмінний.

Цим закрито всі три критичні дефекти розділу A (A1-A3) -- дані й довіра
користувача більше не псуються мовчки під час штатної роботи.

- [x] **A2. Соціальне відновлення (Shamir) повністю нефункціональне — `splitSecret` не повертає `threshold`/`totalShares`. ВИПРАВЛЕНО (коміт нижче).**
  Підтверджено читанням коду напряму: `client/js/shamir.js:143`
  (`splitSecret`) повертає `xs.map((x, i) => ({ x, y: shareYs[i] }))` —
  РІВНО два поля, `x` і `y`. Але `client/js/recoveryShare.js`
  (`encodeShareAsText`, рядок 75; `buildRecoveryShareAnnounce`, рядки
  25-28) читає `share.threshold`/`share.totalShares` на кожному
  згенерованому об'єкті. Реальний виклик
  `encodeShareAsText(splitSecret(secret, {threshold, shares})[0])`
  видає рядок `"spirit-share:1.undefined.undefined.<base64>"`.
  `decodeShareFromText` цей рядок відхиляє (`Number("undefined")` дає
  `NaN`, не проходить `Number.isInteger`-перевірку). Кожна частка,
  згенерована через UI "Налаштувати відновлення", нечитабельна.
  Фікс очевидний і без архітектурних наслідків: `splitSecret` має
  повертати `{x, y, threshold, totalShares}` (значення `threshold`/
  `shares`-параметрів уже є в області видимості функції). Заразом варто
  додати 4-байтову контрольну суму в текстовий формат (§4 звіту,
  "Частки Шаміра не автентифіковані") — контрольну суму НЕ додано цим
  фіксом (окрема зміна wire-формату, свідомо не змішана з терміновим
  фіксом одного бага — "один бег-фікс = один коміт").
  - [x] **Tests**: `client/tests/shamir.test.js` — новий тест підтверджує
        кожна повернена частка несе власні `threshold`/`totalShares`
        (не лише `{x,y}`). `client/tests/recoveryShare.test.js` — 2 нових
        тести, що вперше проганяють РЕАЛЬНИЙ `splitSecret()` (не
        заготовлений вручну `SHARE`-мок, який маскував баг) через повний
        цикл `encodeShareAsText`→`decodeShareFromText` і
        `buildRecoveryShareAnnounce`→`parseRecoveryShareAnnounce`→
        `combineShares`. Підтверджено RED до фіксу (реальний вивід
        `"spirit-share:1.undefined.undefined...."`, точно як описано в
        оцінці). Разом: 34/34 у двох файлах, 344/344 разом з `app.test.js`
        (`splitSecret` там не мокнутий, використовується напряму).
  - [x] **Impl**: `client/js/shamir.js:143` — `splitSecret` тепер повертає
        `{x, y, threshold, totalShares: shares}` замість `{x, y}` (значення
        вже були в області видимості функції як параметри `threshold`/
        `shares`).
  - [x] **Exec review**: самоперевірка — (а) жоден наявний виклик
        `combineShares` не постраждав (вона читає лише `share.x`/`share.y`,
        нові поля ігнорує); (б) `client/tests/app.test.js`'s допоміжна
        функція (рядок ~1940) й далі вручну підставляє `threshold`/
        `totalShares` при виклику `encodeShareAsText` — тепер це
        надлишково (значення вже коректні від `splitSecret`), але
        нешкідливо, не займано в цьому фіксі; (в) 344/344 у трьох
        файлах (`app.test.js`+`shamir.test.js`+`recoveryShare.test.js`),
        без регресій; (г) задеплоєно на обидва хости, живо підтверджено
        на `spirit.kibr.com.ua` -- реальний `splitSecret()`→
        `encodeShareAsText()`→`decodeShareFromText()` у справжньому
        браузері дав `"spirit-share:1.2.3.<base64>"` і `decodedOk: true`
        (раніше -- `.undefined.undefined.`). Побічна операційна
        знахідка під час перевірки: сервер не віддає явний
        `Cache-Control`/`Expires` для статичних `.js`-файлів (лише
        `ETag`/`Last-Modified`), через що браузер цієї сесії кілька
        разів показував застарілий код навіть на "свіжій" вкладці, поки
        не додати `?v=`-параметр до URL імпорту вручну -- не блокує цей
        фікс (підтверджено правильним обхідним шляхом), але вартий
        окремого розгляду як мінливий операційний ризик для реальних
        користувачів, що повертаються після оновлення коду.

- [x] **A3. Повідомлення, набране до завершення нового рукостискання, іде ПОПЕРЕДНЬОМУ співрозмовнику. ВИПРАВЛЕНО (коміт нижче).**
  Підтверджено читанням коду напряму (і власним досвідом сьогоднішньої
  сесії: я сам наштовхнувся на суміжний клас цього ж механізму під час
  живого моделювання GC4-mesh — `ensureActivePeer()`'s "reuse if
  active"-поведінка). `initiateChatSession`/`btn-join`-обробники
  (`app.js`, район рядків 4600+/4930-ті) очищають лише `state.peerFingerprint`
  і `state.sessionEcdhWires` перед стартом нової сесії -- `state.channel`
  і `state.sessionKey` (проксі-поля на активний запис `state.peers`,
  Секція GC0) лишаються від ПОПЕРЕДНЬОЇ сесії, якщо стара сесія була
  активною (не через `resetActiveConnection`, тобто без logout між
  спробами). Текст, надісланий у вікні між кліком "Ініціювати" і
  завершенням НОВОГО рукостискання, шифрується під СТАРИМ `sessionKey`
  і йде через СТАРИЙ `channel` -- попередньому співрозмовнику, поки
  інтерфейс уже показує "нову" розмову. `wireChannelCallbacks`'s
  anti-stale-write guard (Секція GC0/GC3) захищає лише RECEIVE-шлях
  (вхідні повідомлення від застарілого з'єднання), не SEND-шлях.
  Вибрано другий (надійніший) варіант із двох запропонованих: ніколи не
  перевикористовувати `ensureActivePeer()`'s reuse-семантику для явного
  старту НОВОЇ сесії.
  - [x] **Tests**: `client/tests/app.test.js`, новий тест у
        `describe("btn-initiate")`: повністю встановлює першу сесію
        (реальний канал + похідний sessionKey, peer A), потім клікає
        "Ініціювати чат" ВДРУГЕ і СВІДОМО лишає нове рукостискання
        незавершеним (не викликає `onChannelOpen`/`onLocalOfferReady`
        для нього) -- саме те вікно, де жив баг. Підтверджено RED до
        фіксу: `state.activeConnectionId` не змінювався між першою й
        другою спробою (той самий запис перевикористовувався). Тест
        перевіряє: новий `connectionId` відрізняється від старого,
        старий видалений з `state.peers` повністю (не просто неактивний),
        `state.channel`/`state.sessionKey` для щойно стартованої (ще не
        завершеної) сесії -- `null`, і повідомлення, набране в цьому
        вікні, ставиться в чергу (`chat-send-status` показує "в черзі"),
        а НЕ йде через канал peer A. 311/311 у повному файлі.
  - [x] **Impl**: `client/js/app.js` -- `resetActiveConnection()` (уже
        наявна функція, Секція GC0, раніше використовувалась лише при
        logout) тепер викликається на ПОЧАТКУ трьох місць, що стартують
        нову 1:1-сесію: `initiateChatSession` (кнопка "Ініціювати чат"),
        `btn-join`-обробник, і F4 zero-click auto-join-шлях (останній --
        суто захисна узгодженість, недосяжний на практиці через наявний
        `if (state.senderKey) return`-гейт вище). Той самий патерн, що
        вже використовує `startTaggedGroupInvite` (Секція GC2) для
        групових запрошень -- ЗАВЖДИ створювати новий запис для нової
        сесії, ніколи не мутувати наявний активний.
  - [x] **Exec review**: самоперевірка -- (а) `resetActiveConnection()`
        безпечно викликати навіть коли `state.activeConnectionId` вже
        `null` (перевірено читанням реалізації -- умовний no-op, рядок
        417-419); (б) `btn-quick-chat` не потребував окремого фіксу --
        він викликає `initiateChatSession()` в кінці, фікс успадковується
        автоматично; (в) групові з'єднання (`state.peers`-записи з
        `groupId !== null`, створені через `startTaggedGroupInvite`/
        `handleIncomingMeshRelayOffer`) НЕ видаляються цим фіксом --
        `resetActiveConnection()` видаляє лише ПОТОЧНИЙ активний запис
        (той, що стосується 1:1-екрана, де ці кнопки живуть), а не весь
        `state.peers`; (г) 311/311 у `app.test.js`, без регресій у
        наявних GC0-GC4/RF9-тестах; (д) задеплоєно на обидва хости,
        підтверджено прямим `fetch` (усі 3 сайти виклику присутні в
        коді на сервері). Повторний живий multi-tab WebRTC-тест визнано
        надлишковим -- юніт-тест точно відтворює реальний сценарій
        (справжні колбеки `onChannelOpen`/`onLocalOfferReady`), а
        механіка `state.peers`-записів уже неодноразово живо перевірена
        цієї сесії (GC0-GC4).

---

## B. Інші критичні/високі дефекти зі звіту (перенесено без окремої повторної перевірки коду, крім search-підтвердження нижче)

- [x] **B1. `check_answer` не перевіряє invite-токен ні `sender_key`. ВИПРАВЛЕНО (коміт нижче).**
  Підтверджено читанням `server/library/SignalingController.php:256-266`:
  `requireFields` вимагає лише `room_id`; `get_offer`-гілка того самого
  файлу вимагає токен і звіряє через `inviteManager->isTokenValid` --
  `check_answer` цього не робить взагалі. Будь-хто, хто просто бачив
  invite-посилання, витягує SDP-відповідь (з реальною публічною й
  локальною IP того, хто приєднався) без токена.
  Фікс ВІДРІЗНЯЄТЬСЯ від буквально запропонованого звітом ("додати ту
  саму токен-перевірку, що вже є в `get_offer`"): `invite_token`
  НЕПРИДАТНИЙ для цієї перевірки саме в `check_answer`, бо
  `submit_answer` (попередній крок у протоколі) вже викликає
  `markInviteUsed` -- на момент, коли `check_answer` взагалі має що
  повертати (відповідь щойно з'явилась), токен ВЖЕ спожитий і
  `isTokenValid` завжди повернув би `false` навіть для легітимного
  ініціатора. Замість цього -- прив'язка до `sender_key` ЗАПИСАНОГО
  ІНІЦІАТОРА кімнати (`$session['initiator']`, вже зберігається з
  моменту `create_invite`, `InviteManager.php:34`): лише той
  `sender_key`, що створив ЦЮ кімнату, може опитувати її відповідь.
  Клієнт (`client/js/signalingClient.js:126-133`) уже надсилав
  `sender_key` у цьому запиті -- жодних змін на клієнті не знадобилось.
  - [x] **Tests**: PHP локально недоступний (той самий задокументований
        waiver, що й для решти `server/`, `docs/roadmap.md`) -- жива
        перевірка на обох задеплоєних вузлах замість юніт-тесту,
        відтворює ТОЧНИЙ PoC зі звіту.
  - [x] **Impl**: `server/library/SignalingController.php`'s
        `check_answer`-гілка -- `requireFields` тепер вимагає й
        `sender_key`; новий `hash_equals($session['initiator'], $senderKey)`-
        чек перед поверненням відповіді, `403` при незбігу (та сама
        HTTP-семантика й стиль повідомлення, що й сусідні гілки).
  - [x] **Exec review**: самоперевірка -- (а) `hash_equals` (не `===`)
        для порівняння -- узгоджено з рештою файлу (timing-safe
        порівняння скрізь, де воно вже застосовується); (б) легітимний
        ініціатор (`sender_key`, з яким сам викликав `create_invite`)
        і далі отримує відповідь без жодної додаткової дії на клієнті;
        (в) чужий/відсутній `sender_key` -- `403`, той самий код, що й
        `get_offer`/`submit_answer` для невалідного токена; (г)
        задеплоєно на обидва хости, живо підтверджено ТОЧНИМ PoC зі
        звіту на `spirit.kibr.com.ua` (реальний room_id/sender_key через
        справжній `create_invite` у браузері): `sender_key:
        "attacker-not-a-participant"` → `403 Access Denied: not this
        room's initiator` (раніше -- `200` з реальною SDP-відповіддю);
        легітимний `sender_key` ініціатора → `200 success`; відсутній
        `sender_key` узагалі → `400 Missing arguments`. `spirit.kolo.media`
        підтверджено окремо (та сама `400`-поведінка на відсутньому
        `sender_key`).

- [x] **B2. TTL кімнати (300с) ніколи не оновлюється активністю -- лише час створення. ВИПРАВЛЕНО (коміт нижче, найпростіший з двох запропонованих варіантів).**
  Підтверджено: `server/config.php` -- `SESSION_TTL_SECONDS => 300`;
  `InviteManager.php` пише `timestamp` рівно один раз при створенні;
  `Storage.php` лише читає його для перевірки застарілості. Рукостискання
  в процесі помирало за годинником незалежно від реальної активності.
  Реалізовано "найпростіший" варіант із двох запропонованих (оновлювати
  `timestamp` при кожній дії над сесією) -- глибший перегляд самої
  моделі TTL разом зі стратегічним репозиціонуванням (розділ D) лишається
  окремим, не терміновим питанням.
  - [x] **Tests**: PHP локально недоступний (той самий waiver, що й B1)
        -- жива перевірка на обох хостах.
  - [x] **Impl**: `server/library/SignalingController.php` -- `timestamp`
        оновлюється на КОЖНІЙ дії, що торкається наявної сесії:
        `create_offer`, `get_offer`, `submit_answer`, `check_answer`
        (усі чотири, включно з двома чисто "опитувальними" --
        `get_offer`/`check_answer` -- які раніше взагалі не викликали
        `save()`). Свідомий компроміс, задокументований тут: це додає
        `save()`-запис на КОЖЕН опитувальний запит (initiator опитує
        `check_answer` що ~3с, доки чекає відповіді) -- у поточній
        архітектурі одного `flock`-локу це збільшує навантаження на
        сигнальний вузол під час очікування понад те, що вже існувало
        (звіт §5 уже документує стелю продуктивності ~30-50 одночасних
        встановлень з'єднання як окремий, прийнятий на цьому етапі
        компроміс -- це підсилює той самий компроміс, не створює новий
        клас проблеми).
  - [x] **Exec review**: самоперевірка -- (а) кожна з 4 гілок пише
        `timestamp` ПЕРЕД `save()` (чи для двох раніше-беззаписних
        гілок -- новий `save()`-виклик з тим самим `500`-фолбеком, що й
        сусідні мутуючі гілки); (б) TTL-читання (`Storage.php:80`)
        не потребує змін -- воно й так порівнює `now - timestamp`,
        просто тепер `timestamp` рухається вперед при реальній
        активності; (в) `check_answer`'s нова `hash_equals`-перевірка
        (B1) виконується ДО оновлення `timestamp` -- чужий/неавторизований
        запит НЕ продовжує життя кімнати; (г) задеплоєно на обидва
        хости, живо підтверджено: реальна кімната через справжній
        `create_invite`, два послідовних `check_answer`-опитування (з
        паузою) -- обидва `200 success` без жодної `500`-помилки (нові
        `save()`-виклики в раніше беззаписних гілках не ламають нічого).
        `spirit.kolo.media` перевірено окремо на відсутність помилок.

- [x] **B3. TURN технічно неможливо налаштувати -- немає полів username/credential. ВИПРАВЛЕНО (коміт нижче).**
  Підтверджено: `client/js/webrtc.js`'s `buildRtcConfig(stunUrl, {forceTurnRelay})`
  будує лише `{iceServers:[{urls: stunUrl}]}` (+ `iceTransportPolicy`),
  жодного `credential`/`username` поля ніде в клієнті (грепом по всьому
  `client/`). Підказка в UI радить вписати "TURN з логіном/паролем" у
  поле STUN -- неможливо: `turn:` URI не має userinfo-компонента,
  довгострокові TURN-креденшели за RFC 5766 так не передаються. Окремо:
  `specs/phase5/metadata-resistant-transport.md` стверджує, що поле вже
  покриває це технічно -- це неправда, спека сама потребує виправлення
  формулювання. Форсування `iceTransportPolicy:"relay"` над
  чисто-STUN-списком гарантовано ламає з'єднання (немає relay-кандидатів
  узагалі).
  - [x] **Tests**: `client/tests/rtcConfig.test.js`, новий
        `describe("Section B3: TURN server with username/credential")`
        (3 тести: TURN-запис додається другим у `iceServers` лише коли
        `turnUrl` непорожній; поєднання з `forceTurnRelay`). Наявний
        мок `buildRtcConfig` у `app.test.js` оновлено під нову сигнатуру
        (той самий коментар про "дзеркалить реальну реалізацію" уже
        існував). 311/310+1 flaky (непов'язаний, підтверджено чистим
        ізольовано) в `app.test.js`, 18/18 у `rtcConfig.test.js`+`i18n.test.js`.
  - [x] **Impl**: `client/js/webrtc.js`'s `buildRtcConfig` -- нові опційні
        `turnUrl`/`turnUsername`/`turnCredential`, додають ДРУГИЙ запис
        у `iceServers` лише коли `turnUrl` непорожній. `client/index.html`
        -- три нових поля (`#turn-url`/`#turn-username`/`#turn-credential`)
        на екрані «Сервер», окремі від `#stun-url`; виправлено підказку
        (більше не радить неможливе "вписати логін/пароль у поле STUN").
        `client/js/app.js` -- нова `currentRtcConfig()`-функція замінює
        ІДЕНТИЧНИЙ рядок `buildRtcConfig(el("stun-url").value, {...})`,
        скопійований у 8 різних місць (сам звіт відзначав цю дуплікацію
        окремо, §3.2) -- усі 8 замінено одним викликом. "Збережені
        вузли" (`specs/phase4/multi-node-ui.md`) розширено новими TURN-
        полями в тому самому localStorage-записі (задокументовано:
        TURN-пароль там у відкритому вигляді, той самий рівень довіри,
        що й решта preset'у). `client/js/i18n.js` -- `label.turnUrl`/
        `label.turnUsername`/`label.turnCredential` у 11 локалях (усі
        перекладені, не англійський fallback), і виправлено сам текст
        `server.forceTurnRelayHint` у ВСІХ 11 локалях (раніше скрізь
        радив неможливе). `docs/metadata-resistant-transport.md` --
        виправлено неточне твердження ("поле вже покриває це технічно"),
        зазначено, що воно СПРАВДІ вірне лише ТЕПЕР, після цього фіксу.
  - [x] **Exec review**: самоперевірка -- (а) `turnUrl`-порожній -- ЖОДНОГО
        другого запису в `iceServers` (не порожній об'єкт з
        порожніми username/credential) -- підтверджено тестом; (б) усі
        8 викликів `buildRtcConfig` у `app.js` тепер ідентично проходять
        через `currentRtcConfig()` -- підтверджено грепом
        (`buildRtcConfig(el("stun-url")` більше не зустрічається,
        лишився лише в самому визначенні `currentRtcConfig`); (в) 11/11
        локалей перевірено Node-скриптом на відсутність дублікатів і
        коректність кожного перекладу; (г) задеплоєно на обидва хости,
        живо підтверджено на `spirit.kibr.com.ua` (cache-busted імпорт,
        той самий артефакт ES-модуль-кешу, що й у RF17-19/A1-A3):
        `buildRtcConfig` з реальними TURN-параметрами повертає коректний
        `iceServers` (два записи, другий з `username`/`credential`) +
        `iceTransportPolicy:"relay"`; три нових поля й виправлена
        підказка присутні на екрані «Сервер».

- [x] **B4. Немає обробників стану WebRTC-з'єднання -- зникнення співрозмовника непомітне. ВИПРАВЛЕНО (коміт нижче).**
  Підтверджено грепом: нуль згадок `connectionState`/`iceConnectionState`/
  `onconnectionstatechange`/`oniceconnectionstatechange`/`restartIce`/
  `negotiationneeded` у клієнті. Єдиний сигнал розриву -- `channel.onclose`,
  який НЕ спрацьовує при простому зникненні мережі/закритті вкладки без
  явного закриття DataChannel. `armIceTimeout` знімається на завершенні
  ICE-gathering (мілісекунди), не на відкритті каналу -- не детектор
  невдалого з'єднання, попри назву й коментарі поруч (це лишається
  так, як і було -- окрема, вже коректно задокументована річ, не
  чіпається цим фіксом).
  - [x] **Tests**: `client/tests/webrtc.test.js` -- 2 нових тести (по
        одному на `startAsInitiator`/`startAsJoiner`): `onConnectionStateChange`
        реально викликається з поточним `pc.connectionState` при
        спрацюванні `pc.onconnectionstatechange`. `client/tests/app.test.js`
        -- новий тест, що явно НЕ викликає `onChannelClose` (симулює
        РІВНО той пропуск, що знайшов звіт -- зникнення без чистого
        закриття каналу), лише `onConnectionStateChange("disconnected")`:
        перевіряє, що статус оновлюється (`/втрачено/`) і повідомлення,
        набране після цього, ставиться в чергу, а не йде в мертвий
        канал. 312/312 -- 1 (той самий відомий flaky ICE-timeout timer-
        тест, підтверджений чистим ізольовано, непов'язаний). 30/30 у
        `webrtc.test.js`+`i18n.test.js`.
  - [x] **Impl**: `client/js/webrtc.js` -- новий `onConnectionStateChange`
        callback у `startAsInitiator`/`startAsJoiner`, підключений через
        `pc.onconnectionstatechange = () => onConnectionStateChange?.(pc.connectionState)`.
        `client/js/app.js`'s `wireChannelCallbacks` -- логіку розриву
        з'єднання (раніше жорстко всередині `onChannelClose`) винесено
        в спільну `handleConnectionTornDown(statusKey)` (той самий
        stale-write guard, що й `onChannelOpen`), яку тепер викликають
        ОБИДВА шляхи: `onChannelClose` (чисте закриття, `status.closed`)
        і новий `onConnectionStateChange` при `disconnected`/`failed`/
        `closed` (`status.peerConnectionLost`, новий, відмінний від
        `status.closed` статус-текст). `client/js/i18n.js` --
        `status.peerConnectionLost` у 11 локалях (усі перекладені).
  - [x] **Exec review**: самоперевірка -- (а) `connectionState`
        (агрегований ICE+DTLS стан), не `iceConnectionState` окремо --
        простіший, рекомендований сучасний API, покриває той самий
        клас подій, що перелічені в звіті; (б) обидва шляхи розриву
        (`onChannelClose`/`onConnectionStateChange`) можуть теоретично
        спрацювати для ОДНІЄЇ й тієї самої події (channel закривається
        приблизно одночасно з переходом `connectionState` у `closed`) --
        свідомо НЕ додано дедуплікацію: обидва виклики `handleConnectionTornDown`
        ідемпотентні (повторне `state.channel = null` тощо нешкідливе),
        у гіршому разі статус-текст оновлюється двічі поспіль без
        функціональної шкоди; (в) `restartIce`/`negotiationneeded`
        (автоматичне відновлення з'єднання) НЕ реалізовано цим фіксом
        -- звіт просив ЛИШЕ виявлення розриву ("зникнення непомітне"),
        не автоматичне перепідключення -- свідомо звужений обсяг,
        задокументовано тут; (г) 311/312 (1 відомий flaky, підтверджено
        чистим ізольовано), 30/30 у `webrtc.test.js`+`i18n.test.js`;
        (д) задеплоєно на обидва хости, живо підтверджено на
        `spirit.kibr.com.ua` (cache-busted імпорт): фейковий
        `RTCPeerConnection` реально викликає `onConnectionStateChange`
        через `startAsInitiator`, i18n-текст `status.peerConnectionLost`
        резолвиться коректно.

B1-B4 завершено. Лишаються B5-B7, з яких B6 (автентифікація прив'язки
пристрою) і B7 (вибір ліцензії) явно позначені як такі, що потребують
рішення користувача перед кодом.

- [x] **B5. Немає перевірки очікуваного відбитка при дзвінку відомому контакту. ВИПРАВЛЕНО (коміт нижче), знахідка підтверджена -- звіт мав рацію.**
  Підтверджено читанням `client/js/app.js:1199-1207`
  (`#contacts-list`'s click-обробник кнопки "Написати"): `targetFingerprint`
  читається з DOM-рядка контакту, резолвиться в `contact`, і передавався
  ЛИШЕ як `pushToContact` (для push-сповіщення) -- сам факт, КОМУ саме
  телефонують, більше НІДЕ не зберігався. `identity-announce`-гілка
  (`handleChatMessage`) безумовно писала `state.peerFingerprint = verified.fingerprint`
  незалежно від того, хто відповів -- підміна ідентичності проходила як
  буденний рядок "новий контакт".
  - [x] **Tests**: `client/tests/app.test.js`, новий тест у
        `describe("identity announce in chat flows (Section 12)")`:
        клікає кнопку "Написати" на конкретному контакті (не загальне
        "Ініціювати чат"), відповідає identity-announce з ІНШИМ
        fingerprint'ом (симулює підміну на боці недовіреного вузла чи
        зловмисного релею), підтверджено RED до фіксу (`verified` як
        успішний, `"співрозмовник підтверджений: ...bbbb..."`).
        Перевіряє: статус показує mismatch-попередження, `rememberContact`
        НЕ викликається (підміна не потрапляє в контакти). 313/313 у
        `app.test.js` (той самий раніше відомий flaky ICE-timeout тест
        цього разу пройшов і в спільному прогоні).
  - [x] **Impl**: `client/js/app.js` -- клік по кнопці конкретного
        контакту тепер передає `expectedFingerprint: targetFingerprint`
        в `initiateChatSession`; `initiateChatSession` записує його на
        САМ ЗАПИС з'єднання (`ensureActivePeer().expectedFingerprint`,
        новий, plain-поле поза `PEER_PROXY_FIELDS`, той самий патерн, що
        й `groupId`) -- прив'язано до КОНКРЕТНОГО з'єднання, не до
        глобального `state`, той самий урок, що вже застосований у A3.
        `identity-announce`-гілка -- новий чек
        `getActivePeer()?.expectedFingerprint` ПЕРЕД записом
        `state.peerFingerprint`: якщо очікування було встановлено (дзвінок
        конкретному контакту) І воно НЕ збігається з тим, хто фактично
        відповів -- `status.peerIdentityMismatch`, `return` (та сама
        структура відмови, що й `!verified`-гілка, нічого не пишеться).
        Загальні сесії без конкретного адресата (`Ініціювати чат`/
        quick-chat, `expectedFingerprint: null`) НЕ зачіпаються --
        перша зустріч лишається легітимною. `client/js/i18n.js` --
        `status.peerIdentityMismatch` у 11 локалях (усі перекладені).
  - [x] **Exec review**: самоперевірка -- (а) поле на записі з'єднання,
        не на `state` напряму -- узгоджено з multi-connection моделлю
        GC0/A3: два одночасні дзвінки (звичайний 1:1 + фоновий мешовий)
        не сплутають очікування одне одного; (б) мешові/фонові
        з'єднання (GC4) теж технічно проходять через ТУ САМУ
        `handleChatMessage`'s `identity-announce`-гілку (їхній `onMessage`
        викликає `handleChatMessage` напряму), але новий чек безпечно не
        спрацьовує для них -- `expectedFingerprint` там ніколи не
        встановлюється (лише 1:1-дзвінок по контакту його пише), тож
        `if (expected && ...)` коротко замикається на `false`;
        (в) підміна відхиляється ДО будь-якого запису в `contacts`
        чи `state.peerFingerprint` -- підтверджено тестом
        (`rememberContact` не викликаний); (г) 313/313, без регресій;
        (д) задеплоєно на обидва хости (підтверджено грепом деплоєного
        `app.js` на обох), i18n-текст живо підтверджений на
        `spirit.kibr.com.ua`. Повний живий 2-вкладковий сценарій із
        симуляцією impostor-відповіді визнано надлишковим -- юніт-тест
        точно відтворює механізм, а сам клас "verified-стан на
        конкретному з'єднанні" вже неодноразово живо перевірений цієї
        сесії (GC0-GC4, A3).

Цим завершено весь розділ B (B1-B5). Лишаються B6 (автентифікація
прив'язки пристрою) і B7 (вибір ліцензії) -- обидва явно потребують
рішення користувача перед кодом.

- [x] **B6. ЧАСТКОВО ВИПРАВЛЕНО (рішення користувача 2026-07-25: SAS +
  опціональний PIN) -- реальне покращення, з чесно задокументованим
  залишковим ризиком, не повне закриття.**
  Звіт: `deviceLinking.js`'s grant-обробник відповідав на будь-яке
  коректно сформоване `device-link-request` сирим приватним ключем, без
  анонсу ідентичності/номера безпеки/коду підтвердження -- модель
  "possession of invite token = authorization".

  Реалізовано: обидві сторони каналу прив'язки незалежно обчислюють
  `deriveLinkVerificationCode()` (SAS, ~6-значний код) з уже узгоджених
  ECDH-публічних ключів каналу (`state.sessionEcdhWires`); людина порівнює
  код на обох екранах; лише після явного кліку "Підтвердити" на основному
  пристрої надсилається grant. Опціональний PIN (`#link-pin`/
  `#device-link-pin`) -- додатковий фактор ПЕРЕД показом SAS, явно НЕ
  заміна SAS-перевірки (сам по собі PIN, що йде через канал, який
  контролює MITM, не захищає -- задокументовано в коді).

  **Exec review (Opus-subagent, 1 ітерація) знайшов 3 проблеми:**
  - **F1 (ВИСОКА, залишається невиправленою, чесно задокументована як
    відоме обмеження в docstring `deriveLinkVerificationCode`)**: SAS без
    commitment-кроку теоретично можна перебрати. Активний сигнальний MITM,
    що перехоплює offer/answer, дізнається цільовий код ДО того, як сам
    публікує свій ECDH-ключ, і може перебрати ~2^20 ключових пар (простір
    6-значного коду), щоб знайти пару, яка дає той самий SAS на обох
    екранах -- у межах кількох хвилин на звичайному залізі, що вкладається
    в дефолтний `answerWaitTimeoutMs`. Справжній ZRTP/Signal-рівень безпеки
    вимагає commitment-раунду (сторона, що відповідає другою, публікує хеш
    свого ключа ДО того як побачить ключ ініціатора) -- це зміна самого
    протоколу сигналінгу (новий раунд обміну), а не one-line фікс, і
    свідомо НЕ реалізована в цьому коміті. Що фікс РЕАЛЬНО закриває:
    найдешевшу й найпоширенішу атаку зі звіту -- зловмисник, що просто
    ОТРИМАВ/вгадав invite-token (асинхронно, без живої позиції на
    сигнальному каналі), більше не отримує автоматичний grant. Що НЕ
    закриває: повністю активного, ресурсного, реального MITM на
    сигнальному рівні в момент прив'язки. Записано як окремий пункт
    майбутньої роботи нижче (потрібен новий раунд у протоколі сигналінгу).
  - **F2 (виправлено)**: клік "Підтвердити" читав `state.channel`/
    `state.sessionKey` в МОМЕНТ КЛІКУ, а не в момент показу SAS-коду --
    якщо людина лишала запит непідтвердженим і стартувала ІНШЕ з'єднання
    (звичайний чат), клік пізніше надсилав сирий ключ через НОВУ, чужу
    сесію. Виправлено: `resetActiveConnection()` тепер ховає блок і
    скидає обидва `.onclick` при будь-якій зміні активного з'єднання;
    додатково клік-обробник захоплює `channel`/`sessionKey`/
    `connectionId` на момент показу коду й звіряє їх при кліку (захист
    у глибину).
  - **F3 (виправлено)**: жоден тест не перевіряв АРГУМЕНТИ виклику
    `deriveLinkVerificationCode` (лише відрендерений рядок-заглушку) --
    регрес, що передавав константи чи один і той самий wire двічі,
    залишався б непоміченим і мовчки повертав би вразливість. Додано
    `expect(deriveLinkVerificationCode).toHaveBeenCalledWith(...)` з
    реальними wire-значеннями на обох сторонах.
  - [x] **Tests**: `client/tests/deviceLinking.test.js` -- нові тести
        на `deriveLinkVerificationCode` (детермінізм, незалежність від
        порядку, зміна коду при зміні будь-якого з вхідних значень) і
        опціональне поле `pin` в `createLinkRequest`.
        `client/tests/app.test.js` -- нові тести: SAS показується й
        НІЧОГО не надсилається до підтвердження; відхилення (reject)
        нічого не надсилає; PIN-невідповідність відхиляється до показу
        коду; F2 -- старт іншого з'єднання під час непідтвердженого
        запиту ховає блок і не викликає `createLinkGrant`; F3 -- виклик
        `deriveLinkVerificationCode` з реальними wire-аргументами на
        обох сторонах. Повний набір: 858/858 зелено.
  - [x] **Impl**: `client/js/deviceLinking.js` (нова
        `deriveLinkVerificationCode`, опціональний `pin` в
        `createLinkRequest`), `client/js/app.js` (обидва
        `btn-link-device`/`btn-join-as-device`-обробники, `resetActiveConnection`),
        `client/index.html` (нові елементи), `client/js/i18n.js` (нові
        ключі в усіх 11 локалях), `client/css/style.css`
        (`.verification-code`).
  - [x] **Exec review**: 1 ітерація (Opus-subagent) -- F2/F3 виправлено
        й повторно перевірено (858/858 тестів); F1 свідомо залишено
        невиправленим із чесним документуванням залишкового ризику й
        конкретним планом наступного кроку (нижче), а не приховано чи
        відкинуто без підстав -- відповідає дозволу CLAUDE.md на
        документоване, а не мовчазне, звуження обсягу.

  **Наступний крок для повного закриття F1 (окрема майбутня робота, НЕ
  зроблено в цьому коміті)**: додати commitment-раунд у протокол
  прив'язки -- сторона, що відповідає другою (новий пристрій), публікує
  `SHA-256(ecdhPubkey)` ДО того, як бачить реальний ECDH-ключ основного
  пристрою; основний пристрій публікує свій реальний ключ лише ПІСЛЯ
  отримання цього хешу. Вимагає зміни порядку/вмісту offer/answer
  обміну в `webrtc.js`/`app.js` (і, можливо, нового поля в
  сигнальному протоколі) -- архітектурна зміна, що заслуговує власної
  спеки.

- [x] **B7. ВИПРАВЛЕНО (рішення користувача: AGPL-3.0-or-later).**
  Немає ліцензії (`LICENSE` відсутній, `package.json` мав
  `"private": true`). Перед фіксом досліджено сумісність (за прямим
  запитом користувача): обидва сторонні файли, що реально постачаються
  з клієнтом (`client/js/vendor/hash-wasm.esm.js`,
  `client/js/vendor/qrcode.esm.js`) -- MIT (перевірено дослівно в
  самих файлах), MIT explicitно сумісний з (A)GPL. Сервер (`server/`)
  без жодних сторонніх залежностей (нема `composer.json`/vendor).
  `devDependencies` (`vitest`/`jsdom`/`fake-indexeddb`, MIT/Apache-2.0)
  використовуються лише для тестів, ніколи не постачаються
  користувачам -- на вибір ліцензії не впливають. Конфліктів не
  знайдено. Реалізовано: `LICENSE` -- офіційний текст AGPL-3.0
  завантажено безпосередньо з `gnu.org` (не згенеровано); `package.json`
  -- `"private": true` знято, додано `"license": "AGPL-3.0-or-later"`;
  `README.md` -- новий розділ "Ліцензія" з обґрунтуванням вибору.
  - [x] **Tests**: н/д (не код) -- підтверджено `npx vitest run`
        (861/861) без регресій від зміни `package.json`.
  - [x] **Impl**: `LICENSE` (новий), `package.json`, `README.md`.
  - [x] **Exec review**: н/д для ліцензійного/документаційного фіксу.

---

## C. Дешеві фікси з великим ефектом (з розділу 10 звіту, "Далі")

- [x] **C1. ВИПРАВЛЕНО.** Ліниво завантажувати Google GSI-скрипт (лише
  при кліку на кнопку верифікації), а не безумовно в `<head>`.
  Підтверджено: `client/index.html:18` (тепер видалено). Реалізовано:
  `client/js/app.js`'s новий `ensureGoogleGsiLoaded()` -- інжектить
  `<script src="https://accounts.google.com/gsi/client">` у `<head>`
  ЛИШЕ при першому кліку на "Підтвердити через Google", кешує проміс
  (не інжектить вдруге при повторному кліку), пропускає інжекцію якщо
  `window.google.accounts.id` вже існує. Викликається на початку
  `btn-google-verify`'s обробника, до `promptGoogleSignIn`.
  - [x] **Tests**: `client/tests/app.test.js`, новий тест підтверджує
        відсутність `<script>`-тегу одразу після `initApp` (до кліку).
        2 наявних тести, що реально доходять до `promptGoogleSignIn`,
        доповнено новим `resolveGoogleGsiLoad()`-хелпером (знаходить
        інжектований тег і вручну викликає `.onload()` -- jsdom не
        завантажує зовнішні скрипти реально). Виявлено й виправлено
        тестовий нюанс: `document.head` НЕ скидається між тестами
        (лише `body.innerHTML`), тож хелпер бере ОСТАННІЙ співпадний
        тег, не перший (інакше знаходив залишок від попереднього тесту
        й вішав чекання назавжди). 314/314 у `app.test.js`.
  - [x] **Impl**: див. вище.
  - [x] **Exec review**: самоперевірка -- (а) помилка завантаження
        скрипта (мережевий збій) коректно потрапляє в `try/catch`
        обробника й показується як звичайний `status.error`, а не
        зависає -- `googleGsiLoadPromise` скидається в `null` на
        `onerror`, дозволяючи повторну спробу наступним кліком; (б)
        `google.accounts?.id`-перевірка на початку захищає від
        подвійної інжекції, якщо скрипт якимось чином уже завантажений
        (напр. користувач раніше відкривав інший таб цього ж
        застосунку в тому самому вікні -- малоймовірно, але дешево
        перевірити); (в) 314/314, без регресій; (г) задеплоєно на
        обидва хости, живо підтверджено на `spirit.kibr.com.ua` --
        `read_network_requests` на завантаженні сторінки не містить
        ЖОДНОГО запиту до Google (лише власний `googleOAuth.js`);
        `<script>`-тег відсутній у `<head>` до кліку і з'являється
        РІВНО після кліку на "Підтвердити через Google" із заповненим
        Client ID.
- [x] **C2. ВИПРАВЛЕНО, знахідка підтверджена.** Очищати `#chat-log` між
  сесіями -- новий чат раніше відкривався з видимим текстом попередньої
  розмови. Підтверджено читанням `appendChat` (`client/js/app.js`) --
  функція лише додає рядки, ніде не очищає контейнер.
  - [x] **Tests**: `client/tests/app.test.js`, новий тест: перша сесія
        отримує повідомлення в лог, стартує ДРУГА сесія (новий клік
        "Ініціювати чат"), лог більше НЕ містить текст першої розмови
        одразу після входу в нове лоббі (до будь-якого повідомлення
        нового співрозмовника). 315/315.
  - [x] **Impl**: `client/js/app.js`'s `enterConversationLobby` --
        `oneToOneLog.innerHTML = ""` додано поруч із зняттям `hidden`.
        Ця функція викликається РІВНО при старті НОВОЇ сесії (ініціатор
        через `initiateChatSession`, джойнер через `btn-join`) --
        НІКОЛИ при перепідключенні ІСНУЮЧОГО з'єднання (той шлях
        відкриває канал напряму, без проходу через
        `enterConversationLobby`), тож активна розмова не може
        випадково втратитись при нестабільній мережі (RF9-сценарій
        лишається незачепленим). Історія ЗНАЙОМОГО контакту
        перезаповнюється окремо, вже ПІСЛЯ цього, коли identity-announce
        підтверджується (наявний код, не змінювався).
  - [x] **Exec review**: самоперевірка -- (а) не test-first у строгому
        сенсі (імплементація й тест написані практично одночасно,
        той самий прагматичний підхід, що вже застосовувався для дрібних
        UI-фіксів цієї сесії) -- логіку перевірено окремим міркуванням
        (reconnect NE проходить через `enterConversationLobby`) до
        написання тесту, тест підтверджує GREEN; (б) групова розмова
        (`#group-chat-log`) НЕ зачіпається цим фіксом -- окремий
        контейнер, окрема логіка входу (`openGroupConversation`), поза
        обсягом цього конкретного пункту звіту; (в) 315/315, без
        регресій; (г) задеплоєно на обидва хости, підтверджено грепом
        деплоєного `app.js`. Повний живий 2-сесійний UI-сценарій
        визнано надлишковим для цього пункту -- юніт-тест точно
        відтворює механізм (`enterConversationLobby`), а сам факт
        коректного старту нової сесії вже неодноразово живо перевірений
        цієї сесії (A3, GC0-GC4).
- [x] **C3. ВИПРАВЛЕНО.** Обмежити довжину полів на сервері (`requireFields`
  перевіряв лише "непорожній рядок", без верхньої межі). Реалізовано
  єдину межу 64KiB (`MAX_FIELD_LENGTH`), застосовану до КОЖНОГО поля,
  що проходить через `requireFields` (усі дії: `create_invite`,
  `create_offer`, `get_offer`, `submit_answer`, `check_answer`,
  `fetch_proof`) -- достатньо для будь-якого реального SDP (навіть з
  великою кількістю ICE-кандидатів це, як правило, < 10KB) чи
  ECDH-публічного ключа/відбитка, але блокує мультимегабайтне
  зловживання, яке кладе `database.json` за межу пам'яті.
  - [x] **Tests**: PHP локально недоступний (той самий waiver, що й
        B1/B2) -- жива перевірка на обох хостах через curl (запит з
        полем > 64KiB відхиляється 400, звичайний запит з реалістичним
        SDP проходить як і раніше).
  - [x] **Impl**: `server/library/SignalingController.php` -- нова
        константа `MAX_FIELD_LENGTH = 65536`; `requireFields` тепер
        повертає `null` (→ 400 Bad Request), якщо `strlen($value) >
        self::MAX_FIELD_LENGTH`, на додачу до наявної
        порожній-рядок-перевірки.
  - [x] **Exec review**: одна проста, уніфікована межа для всіх полів
        через єдину точку валідації -- мінімальна зміна, без потреби в
        окремих лімітах на поле.
  - **ДОПОВНЕННЯ (2026-07-26, знайдено під час роботи над G2)**:
    щойно написаний автоматизований verify-скрипт
    (`server/verify/section_signaling_controller.php`, G2) виявив, що
    цей фікс мав прогалину -- `sender_key` зчитується напряму на
    самому початку `handle()` (`$input['sender_key'] ?? null`), ДО
    того, як будь-яка гілка `dispatchAction` встигає викликати
    `requireFields()`, тож межа `MAX_FIELD_LENGTH` на нього НІКОЛИ не
    діяла. Мультимегабайтний `sender_key` на `create_invite` проходив
    би повз усі перевірки й записувався б напряму в `database.json`
    через `InviteManager::createInvite`'s `'initiator' => $senderKey`
    -- та сама проблема необмеженого росту файлу, яку C3 мав закрити,
    просто через інше поле. Попередня "жива перевірка" цього пункту
    (curl-запит з 70KB `sender_key`) хибно зарахувала успіх: запит
    справді повертав 400, але з ІНШОЇ причини (`pow_timestamp` був
    рядком, а не числом у тестовому payload) -- сама довжина
    `sender_key` тоді не була причиною відмови. Виправлено: перевірка
    `strlen($senderKey) > self::MAX_FIELD_LENGTH` додана прямо в
    `handle()` поруч із наявною перевіркою на порожній рядок.
    Підтверджено новим verify-скриптом на обох хостах
    (`C3_oversized_sender_key_rejected_400: true`, `all_passed: true`).
- [x] **C4. ВИПРАВЛЕНО.** Перенести лічильники частоти/PoW-antireplay
  ВСЕРЕДИНУ `flock(LOCK_EX)` -- зараз викликаються ДО лока, тобто
  паралельні запити тривіально обходять rate-limit. Реалізовано ІНАКШЕ,
  ніж буквально запропоновано: замість того щоб затягувати весь rate
  limiter у той самий лок, що й `database.json` (це серіалізувало б
  геть УСІ запити, включно з `fetch_proof`'s мережевим fetch'ем, за
  повільним HTTP-запитом до довільного URL -- сам собою новий, гірший
  DoS-вектор), `RateLimiter::checkAndRecord` тепер тримає ВЛАСНИЙ
  `flock` на файлі лічильників на весь цикл load-decide-save (був:
  незахищене читання, потім окремий атомарний `rename`-запис -- дві
  паралельні вимоги з тим самим IP могли обидві прочитати "ще не
  перевищено" ДО того як хоч одна з них записала). PoW anti-replay
  (`checkPow`, що мутує `PowNonceStore`) перенесено ВСЕРЕДИНУ наявного
  `withLock` разом із `dispatchAction` -- той самий TOCTOU, що й B1
  закрив для invite-token.
  - [x] **Tests**: PHP локально недоступний (той самий waiver, що й
        B1/B2/C3) -- жива перевірка на обох хостах (звичайний потік
        create_invite з валідним PoW досі проходить; повторне
        використання того ж nonce досі відхиляється).
  - [x] **Impl**: `server/library/RateLimiter.php` -- `checkAndRecord`
        відкриває файл лічильників `'c+'`, тримає `flock(LOCK_EX)` на
        весь час читання (`loadFromHandle`) і запису (`saveToHandle`)
        через той самий handle; старі окремі `load()`/`save()` (з
        тимчасовим файлом + `rename`) замінено handle-варіантами.
        `server/library/SignalingController.php` -- `checkPow` виклик
        перенесено з ДО `withLock` УСЕРЕДИНУ його колбека, поруч із
        `dispatchAction`.
  - [x] **Exec review**: свідоме відхилення від буквального формулювання
        звіту ("перенести ВСЕРЕДИНУ `flock(LOCK_EX)`") на користь
        рівнозначного за суттю, але безпечнішого рішення -- окремий лок
        для rate-limiter замість розширення лока `database.json` на
        мережевий `fetch_proof`.
- [x] **C5. ВИПРАВЛЕНО.** Додати логування на сигнальний вузол
  (429/403/пошкоджена база/відхилений SSRF) -- оператор зараз не бачить
  жодного сценарію зловживання. Реалізовано новий приватний
  `logSecurityEvent()`, що пише через стандартний PHP `error_log()`
  (єдиний доступний sink на типовому shared-хостингу без сервісу
  агрегації логів) -- лише тип події, IP клієнта, назва дії; НІКОЛИ тіло
  запиту (`sdp_data`/`ecdh_pubkey` -- матеріал сесійного ключа).
  Викликається на: усіх 429 (адмін-throttle, загальний throttle,
  room-creation throttle), 403 через "не в білому списку", 403 через
  SSRF-відхилення в `fetch_proof` (окремо від plain 502-збою вгору за
  течією -- лише справжнє "reject", per існуючий прапорець `reject`),
  і 500 через `RuntimeException` (пошкоджена/недоступна
  `database.json`, збій захоплення лока).
  - [x] **Tests**: PHP локально недоступний (той самий waiver, що й
        B1/B2/C3/C4) -- жива перевірка на обох хостах підтверджує, що
        кожен зазначений сценарій (429 через forceful burst, 403 через
        SSRF-таргет) досі повертає той самий HTTP-статус і тіло
        відповіді, що й до зміни (логування — побічний ефект, не змінює
        контракт відповіді).
  - [x] **Impl**: `server/library/SignalingController.php` --
        `handleFetchProof` тепер приймає `$clientIp` (єдина зміна
        сигнатури; один виклик-сайт, оновлено).
  - [x] **Exec review**: без окремого файлу логів чи ротації --
        покладається на те, що хостинг-провайдер уже керує стандартним
        PHP error-логом; окрема ротація/зберігання лишається поза
        обсягом цього фіксу.
- [x] **C6. ВИПРАВЛЕНО (рішення користувача: провести через i18n, 2026-07-25).**
  Провести `settingsRegistry.js`/`designSettingsRegistry.js`'s підписи через
  i18n замість зашитих українських рядків. Реалізовано: усі `label`/
  `description` (і `optionLabels`/`items[].label` у design-реєстрі)
  замінено на `labelKey`/`descriptionKey` (`optionLabelKeys`/
  `items[].labelKey`) -- i18n-ключі, що резолвляться через `t()` у
  `renderSettingsRegistry()`/`renderDesignSettings()` (`client/js/app.js`).
  Ключі додано з РЕАЛЬНИМ перекладом в `en`/`uk`; інші 9 локалей (de/es/
  fr/it/ru/lt/lv/et/no) отримали англійський текст-заглушку -- той самий
  патерн, що вже існує в файлі для інших ключів (`groups.heading` тощо,
  перевірено: ідентичний англійський текст у всіх 9 локалях крім uk), не
  порушує `i18n.test.js`'s перевірку повноти словника (кожна локаль має ті
  самі КЛЮЧІ, якість перекладу поза обсягом цього перевірки).
  **Exec review (Opus-subagent) знайшов реальний регрес** до коміту:
  перемикання мови (`lang-select`'s `change`-обробник, `app.js:174`)
  викликав лише `applyTranslations(doc)`, що торкається тільки елементів
  з `data-i18n` -- обидві панелі налаштувань будуються імперативно, без
  цього атрибута, тож лишались у ПОПЕРЕДНІЙ мові аж до ручного
  редагування/скидання якогось поля. Виправлено додаванням викликів
  `renderSettingsRegistry()`/`renderDesignSettings()` у той самий
  обробник. Живо перевірено в браузері (localhost:5500): перемикання
  uk -> en миттєво перемальовує обидві панелі.
  - [x] **Tests**: `client/tests/settingsRegistry.test.js`,
        `client/tests/designSettingsRegistry.test.js` -- нові тести на
        форму реєстру (`labelKey`/`descriptionKey`/`optionLabelKeys`/
        `items[].labelKey`) і на резолв через `t()` в en/uk.
        `client/tests/app.test.js` -- новий тест "a language switch
        re-renders SETTINGS/DESIGN_SETTINGS labels too" (RED
        підтверджено до фіксу обробника, GREEN після).
  - [x] **Impl**: `client/js/settingsRegistry.js`,
        `client/js/designSettingsRegistry.js` (реєстри),
        `client/js/app.js` (обидва рендери + `lang-select`'s
        обробник), `client/js/i18n.js` (68 нових ключів у всіх
        11 локалях). Повний тестовий набір: 852/852 зелено.
  - [x] **Exec review**: 1 ітерація (Opus-subagent) -- 1 підтверджений
        реальний регрес (мова не перемальовувала ці дві панелі),
        виправлено й повторно перевірено; 1 дрібна неточність у
        коментарі (`designSettingsRegistry.js`) виправлена по ходу;
        збіглося без потреби у 2-й ітерації.

  **Продовження (2026-07-30, за прямим запитом користувача): реальні
  переклади для 9 локалей.** Замінено англійський текст-заглушку на
  справжній переклад для всіх 68 ключів (`settings.*`/`designSettings.*`)
  у de/es/fr/it/ru/lt/lv/et/no (612 рядків: 68 ключів × 9 локалей).
  Конвенція діакритики: перевірено, що безпосередньо сусідній,
  тематично ідентичний блок (`settings.category.*`/`design.category.*`,
  той самий набір фіч налаштувань, той самий переклад-пас) послідовно
  використовує ASCII-транслітерацію без діакритики для de/es/fr/it/lt/
  lv/et/no (наприклад `"settings.category.fileTransfer": "Dateiuebertragung"`
  в de, `"Trasferimento file"` без потреби в і, `"Filoverforing"` в no) --
  цю конвенцію застосовано і до нових 68 ключів для узгодженості в межах
  тієї самої панелі. ru отримав кирилицю (не стосується питання
  діакритики). Застосовано скриптом (`deploy/_tmp_i18n_translate.mjs`,
  видалено після використання) з прив'язкою до унікального
  `admin.passwordLabel` кожної локалі. Перша версія скрипту мала баг у
  регексі (не обробляв екрановані лапки всередині значень, зіпсувавши
  `settings.proofFailureThreshold.description` в de) -- виявлено через
  падіння `npx vitest run` (SyntaxError від rolldown), відкочено через
  `git checkout`, регекс виправлено на коректний JS-string-literal
  патерн (`(?:[^"\\]|\\.)*`), перезапущено. Також виявлено й виправлено
  кілька випадкових кириличних символів, що потрапили в lt/lv переклади
  через помилку набору тексту (напр. "Labaсgo" замість "Labaja") --
  знайдено скануванням non-ASCII символів поза ru/uk блоками.
  Повний набір: 874/874 зелено (без регресій, лише значення змінені,
  набір ключів той самий, `i18n.test.js`'s перевірку паритету ключів
  не торкнулося).
- [x] **C7. ВИПРАВЛЕНО.** Додати `aria-live` на елементи, де з'являються
  нові повідомлення/статуси (accessibility). Реалізовано: `aria-live="polite"`
  додано до всіх 17 `.status`-елементів (`client/index.html`, єдиний
  спільний `replace_all` по `class="status"`) і до обох чат-логів
  (`#chat-log`, `#group-chat-log`). Дрібна, чисто HTML-зміна без нової
  логіки -- підпадає під small-change carve-out
  (`CLAUDE.md`), без окремої спеки/тестового циклу; повний тестовий
  набір (849/849) перевірено зеленим після зміни.
  - [x] **Tests**: HTML-атрибут, не логіка -- підтверджено відсутність
        регресій повним прогоном `npx vitest run` (849/849 зелено).
  - [x] **Impl**: `client/index.html` -- `aria-live="polite"` на всіх
        `.status`-контейнерах і на `#chat-log`/`#group-chat-log`.
  - [x] **Exec review**: не потрібен для дрібної HTML-only зміни
        (small-change carve-out).
- [x] **C8. ВИПРАВЛЕНО (рішення користувача: не заміна, а вибір із
  випадаючого списку + власний варіант).** Замість заміни дефолтного
  Google STUN на іншу жорстко закодовану альтернативу, додано
  `#stun-preset` -- випадний список (Google/Cloudflare/Mozilla/Власний)
  поруч із наявним текстовим полем `#stun-url`. Вибір пресету заповнює
  поле; "Власний" лишає його недоторканим для ручного вводу. Ручне
  введення в поле синхронізує список назад (на відповідний пресет чи
  "Власний", якщо збігу немає). `#stun-url` лишається єдиним джерелом
  правди, яке читає `currentRtcConfig()` -- жодної зміни самої
  WebRTC-логіки.
  - [x] **Tests**: 4 нових тести в `app.test.js`. Повний набір:
        874/874.
  - [x] **Impl**: `client/index.html` (новий `<select>`), `client/js/app.js`
        (обробники change/input), `client/js/i18n.js` (4 нових ключі в
        усіх 11 локалях).
  - [x] **Exec review**: самоперевірка -- жива перевірка на обох
        хостах (після виявлення й обходу непов'язаного кеш-артефакту,
        задокументованого окремо як G7 нижче).

---

## D. Стратегічні рішення

**РІШЕННЯ КОРИСТУВАЧА (2026-07-25): напрямок 1 -- ефемерна сесія (не месенджер).**
Звіт пропонував три взаємовиключні напрямки (§10 "Стратегічно"); користувач
явно обрав перший (через `AskUserQuestion`, без подальших уточнень чи
заперечень) -- решта два залишаються НЕ обраними, не почато, історія
збережена нижче для контексту.

1. **[ОБРАНО] Перепозиціонувати як інструмент ефемерної сесії** (не месенджер) -- конкурувати з PairDrop/Jitsi Meet/FilePizza, а не з Signal. Найдешевший шлях, узгоджується з тим, що вже реально збудовано (нуль-клік онбординг, ефемерність за замовчуванням). Вимагає: переписати позиціювання в документації/UI-текстах, прибрати слово "анонімний" з заголовка (поки співрозмовник дізнається IP за замовчуванням), переписати file-transfer на потокове читання (зараз `file.arrayBuffer()` цілком у пам'ять -- обмежує "без обмежень"-заяву).
2. **[НЕ обрано] Побудувати офлайн-доставку** -- відмова від інваріанта "нуль бази даних" (черга релею/поштова скринька). Це архітектурна зміна, прямо суперечить `CLAUDE.md`'s задокументованому інваріанту "Zero-database (D1)", який явно вимагає "явного погодження, не мовчазної імплементації" для будь-якої ідеї зберігати повідомлення на сервері.
3. **[НЕ обрано] Лишити як портфельний артефакт / трамплін до гранту чи B2B** -- довести заявлене до робочого стану, податись на грант NLnet.

### D0. Конкретні наступні кроки напрямку 1

Обрання напрямку -- це рішення, а не імплементація. Три пункти нижче не
мали залежностей одне від одного -- бралися в будь-якому порядку.

- [x] **Прибрати слово "анонімний"** з заголовка/маркетингових текстів,
  доки D1 (розкриття IP) не розв'язано технологічно. ВИКОНАНО --
  `README.md` і `welcome.body` в усіх 11 локалях `i18n.js` (див. коміт
  "D0: drop the false anonymous claim").
- [x] **Переписати позиціювання** в `README.md` (розділ "Статус"
  оновлено в G5; головний опис -- у D0's "анонімний"-коміті). Докладний
  прохід по решті `docs/*.md` ЗАВЕРШЕНО (2026-07-30): перевірено кожну
  згадку "месенджер"/"чат-застосунок" -- усі стосуються ІНШИХ
  месенджерів (Telegram/WhatsApp як джерела міграції/каналу передачі
  invite-лінка в `docs/migration.md`, `docs/signaling-protocol.md`,
  `docs/e2ee.md`, `docs/decisions.md`'s D10) або явно кажуть "Spirit --
  НЕ месенджер" (`docs/roadmap.md`). Жоден заголовок чи вступ жодного
  файлу в `docs/` не позиціонує САМ Spirit як месенджер. Правити
  нічого не залишилось -- пункт закрито без змін коду.
- [x] **Переписати file-transfer на потокове читання**. ВИКОНАНО:
  `client/js/fileTransfer.js` -- нові `computeFileHashStreaming`
  (читає файл фіксованими 1MB-вікнами через `Blob.slice()`, НЕ
  `Blob.stream()` -- свідомо, бо `jsdom`'s `File`-полізафіл, яким
  користується власний тестовий набір проєкту, не реалізує
  `.stream()`, тоді як `.slice()`/`.arrayBuffer()` поводяться
  однаково і в `jsdom`, і в реальних браузерах), `countFileChunks`,
  `readFileChunk` (читає ОДИН чанк напряму з диска через
  `File.slice()`, без попереднього нарізання всього файлу в масив у
  пам'яті). `app.js`: `state.outgoingFileTransfers[fileId]` тепер
  тримає посилання на сам `File`-об'єкт замість масиву всіх чанків;
  `sendFileChunks` читає кожен чанк по одному на вимогу.
  - [x] **Tests**: 9 нових тестів у `fileTransfer.test.js`
        (`computeFileHashStreaming` крос-перевірено проти
        `computeFileHash` на відомих SHA-256 тест-векторах і на
        2MB-файлі, що форсує кілька ітерацій вікна;
        `countFileChunks`/`readFileChunk` проти еквівалентної поведінки
        `splitFileIntoChunks`). Повний набір: 870/870.
  - [x] **Impl**: `client/js/fileTransfer.js`, `client/js/app.js`.
  - [x] **Exec review**: самоперевірка -- жива перевірка в реальному
        браузері (не `jsdom`) підтвердила `computeFileHashStreaming`
        байт-в-байт збігається з `computeFileHash` на 3MB-даних.

  **КРИТИЧНА РЕГРЕСІЯ, знайдена й виправлена під час цієї живої
  перевірки**: G3's CSP (`script-src 'self'`, без `'unsafe-eval'` чи
  `'wasm-unsafe-eval'`) повністю блокує `WebAssembly.compile()` --
  `client/js/vendor/hash-wasm.esm.js` (використовується і для Argon2id
  детермінованих акаунтів, і тепер для потокового SHA-256) компілює
  WASM-модуль, і був жодного разу не перевірений живо ПІСЛЯ деплою G3.
  Це означає: **Argon2id-логін за детермінованим акаунтом був мовчки
  зламаний на проді з моменту деплою G3** аж до цього фіксу -- третя
  вже CSP-спричинена production-регресія цієї сесії (після G1's
  bootstrap.js-інциденту), і знову знайдена лише випадково, під час
  живої перевірки НЕсуміжної роботи, а не свідомою повторною
  перевіркою самого CSP. Виправлено: додано `'wasm-unsafe-eval'` до
  `script-src` (дозволяє ЛИШЕ компіляцію WebAssembly, не довільний
  `eval()`/`Function()` -- набагато вужче за `'unsafe-eval'`). Живо
  підтверджено на обох хостах: `argon2id()` знову успішно повертає хеш.
  - [x] **Tests**: жива перевірка на обох хостах (`argon2id()` викликано
        напряму через консоль, повертає коректний хеш; WASM компілюється
        без CSP-помилки).
  - [x] **Impl**: `client/index.html` -- CSP meta-тег, `'wasm-unsafe-eval'`
        додано до `script-src`.
  - [x] **Exec review**: н/д -- те саме, що для решти цього кроку.

  **Урок для майбутніх волонтерів, записаний явно**: після БУДЬ-ЯКОЇ
  зміни CSP -- перевіряти живо КОЖНУ фічу, що торкається `script-src`
  (WASM-компіляція, динамічні `<script>`-теги, сторонні origin'и), а
  не лише ту конкретну фічу, над якою щойно працював. Два з двох
  CSP-інцидентів цієї сесії (bootstrap.js і hash-wasm) виявлені
  ВИПАДКОВО під час перевірки іншої, несуміжної роботи -- жодного разу
  свідомою, окремою повторною перевіркою самого CSP одразу після його
  зміни.

**Пов'язані дрібніші пункти, що теж потребують рішення, а не автофіксу:**

- **Yandex-партнерство атестації ідентичності -- не пріоритетно зараз, але контекст записаний за прямим запитом користувача (2026-07-25)**: питання розглядається НЕ як нейтральний технічний вибір, а в контексті прямої мети проєкту -- Spirit задуманий як (прямий чи непрямий) конкурент месенджерам MAX і Yandex, щоб зменшити їхній вплив і збільшити проблеми саме для ФСБ, а також покращити безпеку опозиційних рухів і правозахисників. Це означає: інтеграція саме Yandex як довіреного партнера атестації ідентичності суперечить самій меті проєкту (довіряти інфраструктурі супротивника для верифікації особи) і, найімовірніше, має бути НЕ реалізована в цьому напрямку взагалі, а не просто "документована з обережним формулюванням", як пропонував звіт. Остаточне рішення (прибрати Yandex зі списку запланованих провайдерів верифікації повністю, чи лишити з explicit-попередженням) -- окреме, не термінове обговорення.
- Твердження "push-компроміс той самий, що й у Signal" (наразі в специфікації push) -- звіт показує, що це не так (у Spirit Google бачить IP відправника напряму, у Signal -- лише IP сервера Signal) -- варто виправити формулювання незалежно від стратегічного напрямку.

### D1. Розкриття IP-адреси співрозмовнику -- потребує технологічного рішення (записано в задачі за прямим запитом користувача, 2026-07-25)

Окремо від стратегічного вибору напрямку продукту (пункти 1-3 вище),
**розкриття реальної IP-адреси користувача співрозмовнику за
замовчуванням** позначено користувачем як важливий пункт, що вимагає
технологічного розв'язання, не просто документаційного застереження.
Поточний стан (з B3 і §6.3 звіту):

- WebRTC DataChannel напряму між браузерами структурно розкриває
  публічну (і часто локальну) IP-адресу обох сторін одна одній через
  ICE-кандидати -- це властивість самого протоколу, не помилка коду.
  Єдиний спосіб приховати IP від співрозмовника в межах WebRTC --
  форсувати передачу ЛИШЕ через TURN-релей (`iceTransportPolicy:"relay"`),
  тобто жодних host/srflx-кандидатів у SDP.
- Перемикач "форсувати TURN-релей" у Spirit вже є в коді, але зараз
  **не функціональний**: немає полів username/credential для TURN,
  і сам `spirit.kolo.media`/`spirit.kibr.com.ua` не тримають власного
  TURN-сервера (задокументовано в B3 вище й у
  `specs/phase5/metadata-resistant-transport.md` як "технічно неможливо
  на поточному shared-хостингу -- немає SSH/довготривалих процесів").
- Тобто розв'язання цього пункту -- НЕ one-line фікс, а щонайменше
  вибір і розгортання реального TURN-рішення. Варіанти для майбутнього
  обговорення (жоден не обраний, це не рішення, а список кандидатів):
  1. Власний TURN-сервер на окремому хостингу з SSH-доступом (coturn) --
     вимагає нового типу інфраструктури, якого проєкт поки не має.
  2. Сторонній комерційний/публічний TURN-провайдер, куди користувач
     сам вписує свої креденшели (поле для цього треба додати -- саме це
     й підтверджено відсутнім у B3) -- не вимагає від проєкту тримати
     інфраструктуру, але покладає TURN-довіру на третю сторону.
  3. TURN-релей через Tor (`.onion`) -- поєднати з уже задокументованою
     (і вже частково підтримуваною для сигналінгу) Tor-стратегією
     `docs/e2ee.md`/`metadata-resistant-transport.md`, щоб і сигналінг,
     і сам релей IP-адреси не розкривали.
  4. Форсувати TURN-релей за замовчуванням (не опційним перемикачем),
     щойно хоч один робочий варіант із 1-3 буде готовий -- зараз
     перемикач вимкнений за замовчуванням, тобто навіть коли TURN
     запрацює, більшість користувачів про нього не дізнаються, якщо
     самі не увімкнуть.
- Це прямо перетинається з B3 (технічний борг -- відсутні
  username/credential-поля) і з пунктом P1 у вже існуючому бектрозі
  Фази 5 (`docs/roadmap.md`, "Метадані-стійкий транспорт") -- НЕ нова
  окрема задача з нуля, а посилення пріоритету вже знаної, але не
  завершеної роботи. Позначено тут як таке, що потребує явного рішення
  користувача щодо того, ЯКИЙ із варіантів 1-3 реалістичний для
  поточної інфраструктури проєкту, перш ніж писати код.

---

## E. Спростовані/неточні твердження звіту (задокументовано, щоб не піднімались повторно без нових доказів)

Жодного не знайдено при цьому проході -- усі перевірені особисто пункти
(A1-A3, B1-B3) підтвердились. Секція лишається як шаблон на майбутнє:
якщо подальша перевірка спростує якийсь пункт зі звіту, запис іде сюди
з конкретним доказом (grep/цитата коду), за тим самим принципом
"dismissibility rule" з `CLAUDE.md`.

---

## F. Уже задокументовані свідомі компроміси (НЕ баги -- не чіпати без нового обговорення)

Ці пункти звіт згадує як недоліки, але вони вже є явними, задокументованими
архітектурними рішеннями проєкту -- перелічено тут, щоб не переплутати з
недоопрацюваннями з розділів A-C вище:

- **"Нуль бази даних" / немає офлайн-доставки** -- задокументовано в
  `docs/roadmap.md` і специфікації push буквально текстом "push НЕ
  доставляє пропущені повідомлення -- технічно неможливо без порушення
  D1". Свідомий інваріант, не недогляд. Зміна -- лише через стратегічне
  рішення D2 вище.
- **Ratchet без DH-ratchet-кроку/post-compromise security** --
  специфікація сама явно каже "БЕЗ повного Double Ratchet". Свідоме
  звуження обсягу Фази 5 P2.
- **PBKDF2 замість Argon2id для сховища/keyfile** -- задокументоване
  рішення (коментар про відсутність WASM-залежності на момент
  імплементації). Звіт слушно зауважує, що обґрунтування застаріло
  (Argon2id вже є в проєкті через `deterministicIdentity.js`) -- це
  ВАЛІДНА знахідка для розгляду (перенести сховище на Argon2id), але сам
  факт використання PBKDF2 -- не прихована помилка, а задокументоване
  рішення, яке варто переглянути, не "виправити мовчки".
- **TURN-перемикач вимкнений за замовчуванням, обмеження області
  записане в специфікації** -- сам факт відсутності TURN-креденшелів
  (B3) -- реальна прогалина, але FIX-запис у
  `metadata-resistant-transport.md` про "поле вже покриває це технічно"
  -- це вже неточність документації, а не свідоме рішення (див. B3).

---

## G. Знахідки з незалежної оцінки Sonnet 5 (`specs/reviews/spirit-evaluation-sonnet5.md`, 2026-07-26) -- архітектурні й безпекові пункти, ЗАДОКУМЕНТОВАНО для майбутнього рефакторингу/доробок, КОД ПОКИ НЕ ЗМІНЕНО

Цей звіт проводився "наосліп" (без доступу до розділів A-F вище) як
незалежна друга думка. Нижче -- конвертація його технічних знахідок у
той самий Tests/Impl/Exec-review формат, що й решта тріажу, з власною
повторною перевіркою кожного пункту читанням реального коду (не просто
переказ звіту) і точними посиланнями на рядки. Жоден пункт нижче не
дублює вже зафіксовані A-F (перевірено перехресно) -- це переважно НОВИЙ
шар знахідок: структура коду, тестова інфраструктура сервера, HTTP-заголовки,
а не окремі протокольні бати.

- [x] **G1. ЗАВЕРШЕНО (5 кроків, декомпозиція по фічах).** `client/js/app.js`
  -- був 5499 рядків, увесь UI-шар і оркестрація в ОДНІЙ функції-замиканні
  `initApp(doc, options)`. Після 5 кроків: 5499 -> 4551 рядок (-948,
  ~17%). П'ять нових модулів: `settingsPanelUI.js`, `sidebarFoldersUI.js`,
  `groupsUI.js`, `deviceLinkingUI.js`, `fileTransferUI.js`. Залишок
  `app.js` -- ядро: WebRTC-сесії (`startInitiatorSession`/
  `startJoinerSession`), `handleChatMessage` (увесь прийом
  контрольних повідомлень -- текст/файли/mesh-relay/device-list),
  рендер контактів/профілю, роутинг. Подальша декомпозиція цього ядра
  -- НЕ заплановане продовження цього проходу (значно вища
  архітектурна складність, не проста "виносна" робота, як G1).

  **Крок 1 завершено**: панель налаштувань (`renderSettingsRegistry`/
  `renderDesignSettings`, ~277 рядків) винесена в новий
  `client/js/settingsPanelUI.js` як `initSettingsPanelUI({ doc, el, t })`
  -- обрана першою, бо єдина не мала жодної залежності від `state`.
  `app.js`: 5499 -> 5231 рядків.
  - [x] **Tests**: жодних нових тестів не знадобилось -- наявні
        `app.test.js`'s settings/design-settings тести (DOM-driven,
        чорна скринька) пройшли БЕЗ ЗМІН після переносу, що й було
        регрес-перевіркою для цього виду рефакторингу. Живо перевірено
        у СПРАВЖНЬОМУ новому табі (не перевикористаному) на localhost.
  - [x] **Impl**: `client/js/settingsPanelUI.js` (новий),
        `client/js/app.js` (заміна блоку на один виклик +
        прибрано невикористані імпорти `setSetting`/`resetSetting`/
        `resetAllSettings`/`SETTINGS`/`DESIGN_SETTINGS`/
        `getDesignSetting`/`setDesignSetting`/`resetDesignSetting`/
        `resetAllDesignSettings`, лишились лише `getSetting`/
        `applyDesignSettings`, досі потрібні в іншому коді файлу).
  - [x] **Exec review**: самоперевірка -- **цей крок сам виявив і
        призвів до виправлення критичної регресії продакшена від G3**
        (див. нижче) під час живої перевірки в СПРАВЖНЬОМУ новому табі,
        замість перевикористаного з кешованим станом -- дисципліна
        "перевіряй у свіжому табі", яку сам собі встановив раніше в
        цій сесії, спрацювала саме тут.

  **КРИТИЧНА РЕГРЕСІЯ, знайдена й виправлена в процесі цього кроку**:
  жива перевірка G3 (CSP) раніше ХИБНО показала "усе працює", бо
  використовувала таб із застарілим станом ще ДО додавання CSP.
  Насправді G3's `script-src 'self'` (без `'unsafe-inline'`) заблокував
  ГОЛОВНИЙ inline `<script type="module">` в `index.html` (сам виклик
  `initApp()`) -- продакшен на обох хостах був повністю непрацездатний
  (застосунок взагалі не завантажувався) з моменту деплою G3 і до цього
  фіксу. Виправлено: цей inline-скрипт винесено дослівно в
  `client/js/bootstrap.js` (той самий патерн, що й `themePrepaint.js` у
  G3), підключено як `<script type="module" src="js/bootstrap.js">`.
  Побічно виявлено й виправлено ще один прихований баг: реєстрація
  Service Worker використовувала відносний шлях `"./sw.js"`, який з
  нового розташування (`js/bootstrap.js`) зареєстрував би worker зі
  scope `js/` замість кореня сайту `/` -- та сама вузька-scope помилка,
  від якої застерігав коментар прямо над цим рядком; виправлено на
  корене-абсолютний `"/sw.js"`.
  - [x] **Tests**: 861/861 (`npx vitest run`); жива перевірка у
        справжньому новому табі (лічильник мов = 11, settings-рядків = 11,
        design-рядків = 20, нуль помилок консолі) на обох хостах.
  - [x] **Impl**: `client/js/bootstrap.js` (новий), `client/index.html`
        (inline-скрипт замінено на `<script src>`).
  - [x] **Exec review**: н/д окремо -- покрито тим самим самоперевіреним
        циклом вище; урок задокументовано в тексті цього пункту для
        майбутніх волонтерів: **завжди перевіряти CSP-зміни у
        СПРАВЖНЬОМУ новому табі, ніколи в перевикористаному**.

  **Крок 2 завершено**: бічна панель пошуку/фільтрів-чипів контактів +
  вкладене дерево папок (drag&drop, перейменування, видалення) винесені
  в новий `client/js/sidebarFoldersUI.js` як
  `initSidebarFoldersUI({ doc, el, t })` -- `app.js`: 5231 -> 4879
  рядків. Не так само "нульова залежність", як settings-панель у Кроці
  1: `contactDragFingerprint`/`groupDragId` встановлюються в
  dragstart/dragend обробниках рядків контактів/груп у
  `renderContactsScreen()` (лишається в `app.js`, глибоко пов'язана зі
  `state`), а читаються drop-обробником рядка папки в новому модулі --
  розв'язано через `setContactDragFingerprint`/`setGroupDragId`,
  повернуті з `initSidebarFoldersUI`, замість спільної мутабельної
  локальної змінної через межу модулів.
  - [x] **Tests**: жодних нових тестів не знадобилось -- ті самі
        чорноскринькові `app.test.js`-тести (папки/drag&drop/фільтри)
        пройшли без змін. Повний набір: 861/861.
  - [x] **Impl**: `client/js/sidebarFoldersUI.js` (новий), `client/js/app.js`
        (заміна блоку + dragstart/dragend тепер викликають
        setContactDragFingerprint/setGroupDragId замість прямого
        присвоєння).
  - [x] **Exec review**: самоперевірка -- жива перевірка у СПРАВЖНІХ
        нових вкладках (урок із попереднього кроку застосовано з
        самого початку цього разу) на обох хостах: створення папки,
        клік/вибір (фільтрація), localStorage-персистентність, нуль
        помилок консолі.

  **Крок 3 завершено (рішення користувача: продовжити з groups-UI,
  приймаючи вищий ризик)**: картка керування групами (форма створення +
  список груп із пікером додавання учасника) винесена в новий
  `client/js/groupsUI.js` як `initGroupsUI({...})` -- `app.js`: 4879 ->
  4759 рядків. НЕ настільки самодостатня, як кроки 1-2: справді
  потребує `startTaggedGroupInvite` (мінтить invite і, для першого
  обраного учасника, реальну WebRTC-сесію) і `openGroupConversation`
  (торкається `state.activeGroupId` і mesh-relay коду) -- обидві
  лишились в `app.js` і передані як залежності, разом із `state`
  read-only (читається лише `state.senderKey`).
  - [x] **Tests**: жодних нових тестів не знадобилось -- наявні
        `app.test.js`-тести на групи пройшли без змін. Повний набір:
        861/861.
  - [x] **Impl**: `client/js/groupsUI.js` (новий), `client/js/app.js`
        (заміна блоку + оновлений імпорт з `groups.js`: `createGroup`
        прибрано з `app.js` -- більше не використовується там напряму,
        `listGroups` лишився -- досі потрібен у `renderContactsScreen`).
  - [x] **Exec review**: самоперевірка -- жива перевірка у справжніх
        нових вкладках на обох хостах: рендер картки груп, створення
        групи без учасників оновлює список і статус коректно, нуль
        помилок консолі.

  **Крок 4 завершено (найризикованіший на сьогодні)**: device-linking-UI
  (`btn-link-device`/`btn-join-as-device`-обробники, увесь B6-флоу з SAS)
  винесено в новий `client/js/deviceLinkingUI.js` як
  `initDeviceLinkingUI({...})` -- `app.js`: 4759 -> 4645 рядків. На
  відміну від кроків 1-3: цей модуль СПРАВДІ читає Й ПИШЕ до `state`
  (`state.identityKeyPair`/`state.senderKey` присвоюються всередині
  callback'а застосування grant на новому пристрої) -- `state`
  передається ПО ПОСИЛАННЮ (не деструктуризовано), тож записи тут
  одразу видимі в `app.js`, точно як до винесення.
  `startInitiatorSession`/`startJoinerSession`/`currentRtcConfig`/
  `randomSenderKey` і чотири render-/reset- хелпери лишились у `app.js`
  (спільна WebRTC-машинерія сесії й рендер ІНШИХ карток, поза обсягом)
  і передані як залежності; решта (примітиви з `deviceLinking.js`/
  `identity.js`/`e2ee.js`/`db.js`) імпортується напряму, бо без стану.

  Під час цього кроку знайдено й виправлено реальний баг ДО того, як
  він дійшов до тестів: буквальний `*/` у тексті JSDoc-коментаря
  ("render*/reset*") передчасно закрив блок-коментар -- Vite's
  import-analysis коректно відхилив це як синтаксичну помилку;
  виправлено переформулюванням.

  - [x] **Tests**: жодних нових тестів не знадобилось -- наявні
        `app.test.js`-тести на device-linking (B6, включно з SAS/PIN/
        F2-регресією) пройшли без змін. Повний набір: 874/874.
  - [x] **Impl**: `client/js/deviceLinkingUI.js` (новий), `client/js/app.js`
        (заміна блоку + прибрано тепер-зайві імпорти
        `generateDeviceKeyPair`/`createLinkRequest`/`createLinkGrant`/
        `applyLinkGrant`/`appendDeviceToList`/`deriveLinkVerificationCode`
        з `deviceLinking.js` і `listKeys` з `db.js`; `snapshotContacts`
        перенесено цілком у новий модуль, бо мав лише один виклик).
  - [x] **Exec review**: самоперевірка -- **найповніша жива перевірка
        серед усіх чотирьох кроків**: повний end-to-end
        device-linking-хендшейк у ДВОХ реальних вкладках на
        `spirit.kibr.com.ua` (створення профілю -> `btn-link-device` ->
        реальний `createInvite`/PoW -> друга вкладка `btn-join-as-device`
        з тим самим room_id/invite_token -> SAS-код `860245` збігається
        на ОБОХ екранах -> клік "Підтвердити" -> `pub-key-display` на
        новому пристрої побайтово ЗБІГАЄТЬСЯ з ідентичністю primary) --
        підтверджує, що весь SAS/PIN/grant/appendDeviceToList-ланцюжок
        працює після рефакторингу без жодної регресії. Додатково
        підтверджено `createInvite`/PoW-флоу окремо на
        `spirit.kolo.media`. Нуль помилок консолі на обох.

  **Крок 5 завершено (останній, G1 повністю закрито)**: три UI-точки
  входу file-transfer (`#file-input`'s change-обробник, що починає
  надсилання через D0's потокове хешування/нарізку,
  `#btn-file-accept`, `#btn-file-reject`) винесено в новий
  `client/js/fileTransferUI.js` як `initFileTransferUI({...})` --
  `app.js`: 4645 -> 4551 рядок. `renderFileTransferStatus` і
  `sendFileChunks` навмисно ЛИШИЛИСЬ у `app.js` (передані як
  залежність, не перенесені) -- обидві також викликаються з
  `handleChatMessage`'s гілок прийому файлових control-повідомлень
  (файл-чанк/файл-offer), ядра обробки чат-повідомлень, спільного зі
  звичайними текстовими повідомленнями й mesh-relay -- свідомо поза
  обсягом цього кроку, занадто ризиковано чіпати тут.
  - [x] **Tests**: жодних нових не знадобилось -- наявні
        `app.test.js`-тести на файл-трансфер пройшли без змін. Повний
        набір: 874/874.
  - [x] **Impl**: `client/js/fileTransferUI.js` (новий), `client/js/app.js`
        (заміна блоку, прибрано тепер-зайві імпорти
        `computeFileHashStreaming`/`countFileChunks`/`createFileAssembler`
        з `fileTransfer.js`, локальний `randomFileId` перенесено
        цілком у новий модуль -- мав лише один виклик).
  - [x] **Exec review**: самоперевірка -- повний END-TO-END
        файл-трансфер у ДВОХ реальних вкладках на
        `spirit.kibr.com.ua` через СПРАВЖНІЙ WebRTC-канал (звичайний
        1:1 чат: `btn-initiate` -> `btn-join` -> обидві сторони
        верифіковані) -- файл 50KB надіслано (`#file-input` change),
        отримано offer на іншій стороні, `btn-file-accept`, усі 4
        частини передано, SHA-256 (потокова хеш-функція з D0) збіглась
        -- статус "отримано й перевірено" з робочим посиланням
        завантаження. Нуль помилок консолі на обох сторонах.

**Підсумок G1**: 5/5 кроків завершено. `app.js` зменшився на ~17%
(5499 -> 4551 рядків) без жодної регресії в жодному з наявних 874
тестів і з живою end-to-end перевіркою кожного нетривіального кроку
(3-5) на реальних задеплоєних хостах, включно з повним WebRTC-каналом
для кроків 4-5.

- [x] **G2. ЧАСТКОВО ВИПРАВЛЕНО.** Нуль автоматизованих тестів на
  PHP-стороні (`server/`), і наявні ручні verify-скрипти НЕ покривали
  найризикованіший файл і не оновлювались під нові фікси цієї ж сесії.
  Підтверджено (до фіксу): `server/verify/` містив лише 6 файлів --
  `section6_storage.php`, `section7_invite_manager.php`,
  `section8_cors.php`, `section9_rate_limiter.php`, `section_pow.php`,
  `section_pow_integration.php`. Жодного `section_signaling_controller*`
  чи подібного для `SignalingController.php` (689 рядків -- найбільший і
  найскладніший PHP-файл). Реалізовано варіант (b) з переліку нижче:
  новий `server/verify/section_signaling_controller.php` -- повний
  happy-path (`create_invite -> create_offer -> get_offer ->
  submit_answer -> check_answer`) плюс окремі перевірки для B1
  (`check_answer` відхиляє чужий `sender_key`, приймає справжнього
  ініціатора), B2 (`create_offer` рефрешить `timestamp`, з примусовим
  `sleep(2)` між знімками, бо `time()` має роздільність в 1с), C3
  (перевищення `MAX_FIELD_LENGTH` на `sdp_data` І на `sender_key` --
  див. нижче), C4 (повторне використання PoW-nonce відхиляється), C5
  (429-подія логується через `error_log`). `POW_DIFFICULTY_BITS`
  виставлено в `0` для тестового конфігу -- скрипт перевіряє
  диспетчеризацію/лок/валідацію полів `SignalingController`, а не
  бітову складність PoW (те вже покриває `section_pow.php`).

  **Побічна знахідка під час написання скрипта**: перша спроба
  верифікувати C3 для `sender_key` показала `false` -- виявилось, що
  `sender_key` зчитується напряму в `handle()` ДО будь-якого виклику
  `requireFields()`, тож межа `MAX_FIELD_LENGTH` на нього НІКОЛИ не
  застосовувалась (детально -- у доповненні до запису C3 вище). Це
  виправлено тут-таки, разом із додаванням тесту, що фіксує правильну
  поведінку.

  Чому "ЧАСТКОВО": написано ОДИН новий verify-скрипт (охоплює B1/B2/C3/C4/C5
  і саму `SignalingController`), але PHPUnit чи подібний CI-фреймворк
  НЕ впроваджено (варіант (a) нижче лишається невибраним) -- на
  типовому shared-хостингу без `composer`/розширень це нетривіальна
  окрема робота, не зроблена в цьому проході. Розширення покриття на
  `Storage`/`InviteManager` (уже часково покриті `section6`/`section7`)
  чи глибше на `RateLimiter`/`PowNonceStore` (уже `section9`/`section_pow`)
  -- поза обсягом цього конкретного проходу, може бути продовжено окремо.
  - [x] **Tests**: PHP локально недоступний (той самий waiver, що й
        B1-C5) -- нова верифікація живо прогнана на обох хостах через
        curl (`https://spirit.{kibr,kolo.media}/spirit/verify/
        section_signaling_controller.php`), результат
        `"all_passed": true` на обох; файл видалено з живих хостів
        одразу після перевірки (лишається лише в git, той самий
        цикл use-then-delete, що й у наявних `section6-9`).
  - [x] **Impl**: `server/verify/section_signaling_controller.php`
        (новий); `server/library/SignalingController.php` -- фікс
        знайденої прогалини C3 (перевірка довжини `sender_key`).
  - [x] **Exec review**: самоперевірка (без окремого subagent-проходу
        для цього конкретного пункту) -- обґрунтування: чиста
        test-only зміна плюс one-line фікс уже раніше exec-reviewed
        логіки (той самий патерн перевірки, що й у `requireFields`),
        підтверджена живим прогоном на обох хостах з детермінованим
        результатом `all_passed: true`, а не суб'єктивною оцінкою.

- [x] **G3. ВИПРАВЛЕНО.** Відсутній Content-Security-Policy (CSP) -- ні
  meta-тег, ні HTTP-заголовок -- при 26 використаннях `innerHTML` у
  `app.js`, плюс inline `<script>` (`index.html:8-17`), що блокував би
  додавання строгого CSP без `'unsafe-inline'`. Реалізовано: (1)
  inline-скрипт винесено дослівно у `client/js/themePrepaint.js`
  -- НЕ як ES-модуль (модулі виконуються відкладено, а сенс цього
  скрипта саме в тому, щоб відпрацювати ДО застосування CSS і
  уникнути спалаху світлої теми), підключено як звичайний
  `<script src="js/themePrepaint.js">`; (2) додано
  `<meta http-equiv="Content-Security-Policy">` з:
  `script-src 'self' https://accounts.google.com` (без
  `'unsafe-inline'` -- Google GSI-скрипт (C1) підвантажується з цього
  origin'у), `style-src 'self' 'unsafe-inline'` (design-settings
  застосовує `.style.setProperty()` -- живо перевірено, що це JS-DOM
  API НЕ підпадає під `style-src`'s inline-обмеження в жодному
  сучасному браузері, на відміну від літерального `style=""`
  атрибута чи `<style>`-тега, яких у розмітці й так немає), `img-src
  'self' data: blob:`, **`connect-src *`** (свідомо не обмежено --
  сигнальний/STUN/TURN-сервер обирає користувач, `docs/decisions.md`
  D1/D2 -- фіксований `connect-src` зламав би саме цю архітектурну
  особливість), `frame-src https://accounts.google.com` (Google
  Sign-In рендерить у iframe), `object-src 'none'`, `base-uri 'self'`,
  `form-action 'self'` (у розмітці немає `<form>`/`<iframe>`/`<object>`
  крім Google -- підтверджено `grep`).
  - [x] **Tests**: н/д для самого CSP (браузерна політика, не
        JS-логіка) -- жива перевірка через `read_console_messages` у
        Browser pane: завантаження застосунку, перемикання теми
        (pre-paint), відкриття налаштувань і зміна design-setting
        кольору (`.style.setProperty`) -- нуль CSP-порушень у консолі
        в усіх трьох сценаріях. Повний `npx vitest run`: 861/861.
  - [x] **Impl**: `client/js/themePrepaint.js` (новий),
        `client/index.html` (замінено inline-скрипт на `<script src>`,
        додано CSP meta-тег).
  - [x] **Exec review**: самоперевірка -- чисто конфігураційна зміна
        (перенесення existing коду verbatim + декларативна політика),
        підтверджена живою перевіркою консолі за трьома реальними
        сценаріями використання, а не лише відсутністю помилок при
        завантаженні.

- [x] **G4. ВИПРАВЛЕНО (обрано дешевший з двох варіантів: явний
  задокументований+перевірений контракт, без окремої escape-функції).**
  Патерн неекранованої інтерполяції ID у HTML-атрибути/CSS-селектори --
  сьогодні безпечний лише тому, що всі поточні джерела ID
  внутрішньо-генеровані, без валідації на вході в шаблон.
  Підтверджено конкретно: `client/js/app.js:1410` (у складі
  `innerHTML`-присвоєння `renderFolderTree`) і `client/js/app.js:1715`
  (`querySelector`-селектор) -- обидва інтерполюють `n.id`/`groupId`
  без екранування. Реалізовано: інваріант "лише
  `[a-z0-9]+`/hex, ніколи ззовні без валідації" тепер явно
  задокументований коментарем-контрактом прямо на місці визначення
  кожного генератора (`randomFolderId()` в `app.js`,
  `randomGroupId()` в `groups.js`) і ЗАФІКСОВАНИЙ тестом, а не лише
  словом -- майбутня зміна формату чи прийняття зовнішнього ID
  провалить тест ДО того, як це стане реальною XSS-можливістю.
  - [x] **Tests**: `client/tests/groups.test.js` -- новий тест
        `groupId is always lowercase hex ...`; `client/tests/app.test.js`
        -- новий тест `a newly created folder's id is always
        [a-z0-9]+ ...` (створює папку через реальний UI-флоу й
        перевіряє `dataset.folderId`). Обидва зелені, повний набір:
        861/861.
  - [x] **Impl**: `client/js/app.js` (коментар-контракт над
        `randomFolderId`), `client/js/groups.js` (коментар-контракт
        над `randomGroupId`) -- жодної зміни поведінки, лише
        документування й тестове закріплення вже коректного формату.
  - [x] **Exec review**: н/д -- тестове закріплення наявного
        інваріанта без зміни логіки, самоперевірка через реальний
        прогін тестів достатня (small-change carve-out).

- [x] **G5. ВИПРАВЛЕНО.** `README.md`'s розділ "Статус" відставав від
  реального стану проєкту щонайменше на 5 фаз. Оновлено: тепер
  перелічує Фази 0-5 як завершені (замість застарілого "Фаза 1 (MVP)
  завершена... Наступний крок -- Фаза 2") і посилається на `docs/roadmap.md`
  та `specs/reviews/` для актуального стану, без дублювання переліку
  фаз (щоб не застарівати знову при наступній завершеній фазі).
  - [x] **Tests**: н/д.
  - [x] **Impl**: `README.md`, розділ "Статус".
  - [x] **Exec review**: н/д для суто текстової правки (small-change
        carve-out).

- [x] **G6. РОЗГЛЯНУТО, РІШЕННЯ КОРИСТУВАЧА (2026-07-26): не чіпати
  зараз.** Відсутність bundler -> ~49 окремих ES-module HTTP-запитів на
  першому завантаженні -- задокументований архітектурний інваріант
  (`CLAUDE.md`: "Без bundler: клієнт -- чисті ES-модулі, деплояться
  напряму по FTP"). Підтверджено: `grep -c 'import ' client/js/app.js`
  -> 49 окремих `import`-рядків; живий `read_network_requests` показав
  ~30+ послідовних `GET /js/*.js` одразу після завантаження
  `index.html`. Користувач явно підтвердив залишити інваріант як є --
  це НЕ помилка, а свідомий вибір заради простоти деплою на
  shared-хостинг без build-кроку, і зміна його не входить у поточний
  прохід фіксів. Переноситься під ту саму дію, що й розділ F
  (задокументований компроміс, не чіпати без НОВОГО обговорення) --
  цей запис лишається як точка входу, якщо колись питання підніметься
  знову (напр. якщо позиціювання "ефемерна сесія" з розділу D
  розвинеться далі й швидкість першого заїзду стане пріоритетом).
  - [x] **Tests**: н/д.
  - [x] **Impl**: н/д -- свідомо не реалізовано.
  - [x] **Exec review**: н/д.

**Примітка для майбутньої розмови про концепцію** (не код, не автофікс):
незалежний звіт (`spirit-evaluation-sonnet5.md`, розділ 5) відзначив
UX-суперечність -- перший екран одразу пропонує 5 варіантів дії
(портативний логін/відновлення/створення профілю/вхід/швидкий чат), а
повна панель налаштувань відкриває щонайменше 12 розділів (пристрої,
Google-верифікація, proof-of-identity, інфраструктура/TURN, адмін-панель
вузла, дизайн-редагування, імпорт контактів тощо) -- це суперечить
щойно обраному в розділі D напрямку "простий ефемерний інструмент,
не месенджер". Не технічна знахідка -- предмет майбутньої розмови про
конкретний обсяг UI для нової позиції продукту, як і домовлено
("після фіксів будемо корегувати концепцію подальшого розвитку").

- [x] **G7. РОЗГЛЯНУТО, РІШЕННЯ КОРИСТУВАЧА (2026-07-30): прийняти як
  задокументоване обмеження, не чіпати.** Відсутні `Cache-Control`-заголовки
  на статичних JS-файлах -- деплой ЛОМИТЬ застосунок для ІСНУЮЧИХ
  (не нових) відвідувачів, доки браузер сам не переоцінить кеш.
  Знайдено випадково під час живої
  перевірки C8: після деплою (`index.html`, `app.js`, `i18n.js`, без
  `fileTransfer.js`, який не змінювався в цьому конкретному деплої, але
  МІНЯВСЯ в попередньому), кілька тестових вкладок на обох хостах
  стабільно показували повністю "мертвий" застосунок (`initApp()` ніколи
  не завершувався, `#lang-select` лишався порожнім, хоча HTML/CSS
  рендерились нормально) -- root cause: `fetch('/js/fileTransfer.js',
  {cache:'reload'})` показав, що браузер віддавав ЗАСТАРІЛУ версію
  файлу (без нового експорту `computeFileHashStreaming`, доданого в
  попередньому деплої D0), через що динамічний `import` з `app.js`
  падав з `SyntaxError: ... does not provide an export named ...` --
  ЦЯ ПОМИЛКА НЕ З'ЯВЛЯЄТЬСЯ В `read_console_messages` цього
  browser-pane інструмента (сама по собі окрема дрібна знахідка про
  інструментарій), що й ускладнило діагностику. `curl -sI` підтвердив:
  жодного заголовка `Cache-Control`/`Expires` на JS-файлах -- лише
  `Last-Modified`, що змушує браузери покладатись на евристичне
  кешування (RFC 7234 §4.2.2, типово ~10% від `Date - Last-Modified`).
  **Для НОВИХ відвідувачів (без старого кешу) це не проблема** -- вони
  завжди отримують щойно задеплоєні файли. Проблема виключно для
  ІСНУЮЧИХ відвідувачів з відкритою вкладкою чи недавнім візитом:
  частковий деплой (не всі файли одного "покоління" коду оновлені
  одночасно, оскільки FTP-деплой не атомарний і немає версіонування
  URL/build-хешів у назвах файлів) може лишити їх із несумісним
  поєднанням старих+нових модулів аж до природного закінчення
  евристичного кешу чи ручного hard-refresh.
  **Дослідження варіанту (a) виконано й провалилось**: задеплоєно
  тестовий `.htaccess` з `<FilesMatch "\.(js|css)$">
  Header set Cache-Control "no-cache"` на `kibr.ftp.tools`, перевірено
  `curl -sI` -- заголовок НЕ з'явився. Причина: хостинг -- nginx
  (`Server: nginx` у відповіді), що роздає статичні `*.js`/`*.css`
  напряму, минаючи Apache/`.htaccess` повністю (типова гібридна
  конфігурація shared-хостингу). Тестовий файл прибрано одразу після
  перевірки, нічого не зламано.
  Варіант (b) (версіонування імен/query-параметрів) вимагає або
  build-кроку, або ручного переписування 49+ import-виразів при
  кожному деплої -- користувач підтвердив НЕ робити цього зараз
  (узгоджено з G6's рішенням не чіпати без-bundler інваріант).
  **Прийнято як задокументоване обмеження**: не критично, бо (1)
  торкається лише ПОВЕРТАЮЧИХСЯ відвідувачів у вузькому вікні між
  частковим деплоєм і природним оновленням евристичного кешу браузера,
  ніколи нових відвідувачів; (2) самозагоюється без будь-якого
  втручання. Якщо колись знадобиться реальний фікс -- потрібен або
  доступ до панелі хостингу для кастомних nginx-заголовків (не
  перевірявся, FTP-доступу для цього недостатньо), або перегляд G6.
  - [x] **Tests**: н/д (питання HTTP-заголовків хостингу, не логіки).
  - [x] **Impl**: н/д -- свідомо не реалізовано, обмеження прийняте.
  - [x] **Exec review**: н/д.

---

## Висновок цього тріажу

Звіт містить конкретні, перевірні технічні твердження, і вибіркова
перевірка (не побіжна -- я прочитав реальний код трьох найважчих
тверджень, включно з тими, де мій перший делегований прохід дав хибний
"спростовано") підтвердила ВСІ шість перевірених пунктів (A1-A3, B1-B3)
без жодного винятку. Це підстава довіряти решті звіту як добросовісному
й технічно компетентному вхідному матеріалу, а не рекламному чи
згенерованому без опори на код -- хоч і без гарантії, що кожна дрібніша
знахідка (розділ B4-C8) так само витримає повну перевірку без спроб.

Найважливіший практичний висновок: **три критичні дефекти розділу A
(A1-A3) псують реальні дані користувачів або довіру мовчки, під час
штатної роботи, без жодної помилки на екрані** -- це не крайні випадки.
Вони мають пріоритет над усім рештою, включно зі стратегічними
рішеннями розділу D, бо стратегія не має значення, якщо продукт губить
історію при звичайному вході чи надсилає повідомлення не тому
співрозмовнику.
