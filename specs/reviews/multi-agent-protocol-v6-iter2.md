---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після виправлень iter1)
iter: 2
agent: Opus subagent (чистий контекст; на вхід — файл, критерії приймання, артефакт iter1 для правила не-переподняття)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md
decision: FAIL → 13 нових знахідок, усі виправлено; очікує iter3 (остання за лімітом 7.4)
---

# Контекст

Рев'юер перевірив, що всі 16 виправлень iter1 **приземлились** у тексті (так, 16/16), і
чи вони **ефективні**: 5 з них — ні (#3 частково, #7, #8, #10, #16), бо породили нові
дефекти в сусідніх місцях. Виконав рецепти §5.2, §5.5, §5.6, G7 дослівно в bare-репо з 2–3
клонами. 13 нових знахідок, жодна не повторює iter1. Усі цитати звірено — збігаються.

# Знахідки та рішення

Жодну не дисміснуто.

| # | Рядок (iter2) | Суть | Рішення |
|---|---|---|---|
| 1 | 303 `cat > specs/handoffs/TASK-001.md <<EOF` | Без `mkdir -p` heredoc/commit падають, а `push` порожньої гілки все одно `exit 0` → обидва агенти «отримали» claim (відтворено) | **Виправлено.** `set -e`, `mkdir -p`, перевірка `HEAD != origin/main` перед push, guard на наявну локальну гілку |
| 2 | 24 `ls specs/handoffs/` | Handoff живе в незлитій task-гілці; з `main` видно лише завершені | **Виправлено.** §0/G0: цикл `git show origin/task/<ID>:specs/handoffs/<ID>.md` по всіх `origin/task/*`; §5.3 пояснює, чому |
| 3 | 802 «бачить `ls specs/reviews/` із розділу 0» | Той самий корінь для halt; умова 5.4 неоцінювана | **Виправлено.** §7.4 і 5.4 переписані: читати з вершини `origin/task/<ID>` |
| 4 | 322 `git worktree remove ../wt-TASK-001` | Зсередини worktree — `exit 255`, частково видалений стан; локальна гілка лишається; повторний claim іде в detached HEAD | **Виправлено.** `cd "$REPO"`, `worktree remove --force`, `branch -D`; `set -e` зупиняє рецепт на `switch -c` |
| 5 | 410 «локальна гілка створюється з origin/task/TASK-001» | Якщо локальна вже є — береться вона (гілка того, хто програв) без попередження | **Виправлено.** §5.5: перевірка `show-ref` + `merge-base --is-ancestor` → інакше СТОП; `merge --ff-only` після add; заборона `--force` |
| 6 | 1111 `direct_small_changes: true` | Worker комітить у захищену гілку всупереч 6.10 і §2.3 «виключно» | **Виправлено.** 6.0 оголошено єдиним явним винятком із 6.10 (§2.3, рядок у таблиці з приміткою); binding-коментар пояснює L1 |
| 7 | 476 «чи в task-гілку — визначає `direct_small_changes`» | При `false` зміна не має шляху до інтеграції | **Виправлено.** Гілка `small/<UTC>-<slug>` + G7-lite для master |
| 8 | 999 `CLAIMED\|ACTIVE → ABANDONED   RECLAIM` | Суперечить 5.4 (`STATE=CLAIMED`, новий OWNER) | **Виправлено.** RECLAIM → CLAIMED (новий OWNER); ABANDONED — лише явна відмова/рішення master, термінальний |
| 9 | 637 `CRITERIA_SHA … blob'у критеріїв` | Голий blob без шляху — G7 не може перевірити | **Виправлено.** Поле `CRITERIA=<шлях@blob[,…]>` у claim-коміті, handoff, G7, §9, §14 |
| 10 | 989 «хто виконує — таблиця 6.10» | Немає рядків для → DONE, → BLOCKED, → ABANDONED | **Виправлено.** П'ять рядків додано в 6.10 |
| 11 | 442 «не конфліктують ніколи» | Правда лише для push | **Виправлено.** Кваліфіковано: при push — так; при rebase/merge — як будь-які гілки |
| 12 | 1123 `min_independent: 1` без правила | Незв'язане поле у binding — саме те, що §4 забороняє | **Виправлено.** §8.1 посилається на нього як на кількість для звичайних змін |
| 13 | 115 «перелік ідентифікаторів агентів» | Немає джерела AGENT_ID | **Виправлено.** §2.0: `AGENT_ID`, джерело `agent_id.source` у binding (типово `git config user.name`), унікальність — за людиною, відсутність — СТОП |

# Що рев'юер підтвердив як коректне (iter2)

Гонка claim за рецептом дослівно (переможець `[new branch]`/0, той, що програв
`! [rejected]`/1, локальний `main` обох = `origin/main`); попередження про порожню
гілку (`Everything up-to-date`/0) — точне; §5.6 повністю (STALE → push 0 →
is-ancestor 1 → rebase → FRESH); блок G7; DWIM у §5.5 для свіжого клона; виявлення
дрейфу критеріїв через `rev-parse origin/main:<file>`; семантика refspec і
`pull --ff-only`; проєктна незалежність; усі 13 пунктів «Додано у v6» існують у тексті;
§7.1–7.3, §7.5, §8.2, §11, §13 — без дефектів.

# Залишкова невизначеність

- Усі експерименти — локальні bare-репо на Git for Windows; поведінку хостингів
  (pre-receive, protected branches) не перевіряли.
- Знахідки 6/7/9/13 — конфлікти правил, не спостережуваної поведінки.
