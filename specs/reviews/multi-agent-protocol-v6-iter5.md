---
spec: specs/multi-agent/MULTI-AGENT.md
section: protocol-v6 (весь документ, після редизайну CRITERIA — варіант (а) — і 6 фіксів iter4)
iter: 5
agent: Opus subagent (чистий контекст; на вхід — файл, критерії, артефакти iter1–4; клон у шляху з пробілом)
files-reviewed:
  - specs/multi-agent/MULTI-AGENT.md
  - specs/reviews/multi-agent-protocol-v6-iter1.md … iter4.md
decision: FAIL → 6 нових знахідок (4 дискваліфікуючі); фікси застосовано, чекають на рішення людини щодо iter6
---

# Контекст

Рев'юер перевірив 7 фіксів iter4 + 2 спостереження: **7/9 ефективні**; #4 (CRITERIA) і
#5 (вихід із BLOCKED) приземлились, але недостатні; n2 (refspec) — частково, породив
новий дефект. 6 нових знахідок, жодна не повтор. Цитати звірено.

**Моя помилка у верифікації** (записую, щоб не повторити): T16c перевіряв «сусідня правка
не блокує» на секції, що була ОСТАННЬОЮ у файлі — без порожнього рядка після неї. Тому
різниця «$(...) зрізає кінцеві порожні рядки / awk на main їх лишає» не проявилась.
Рев'юер узяв секцію посеред файлу і зловив. Тест має покривати не-останню секцію.

# Знахідки та рішення

Жодну не дисміснуто. Усі 6 виправлено після рев'ю (до рішення людини про iter6 —
редагування чернетки не є ітерацією рев'ю).

| # | Рядок | Цитата | Суть | Рішення |
|---|---|---|---|---|
| 1 | 751 | `diff .crit-claimed .crit-now && rm …` | Копія без кінцевого порожнього рядка ($(...) зрізає), секція на main — з ним → diff ≠ ЗАВЖДИ; жодна одиниця не проходить G7 (відтворено) | **Виправлено.** `diff -B --strip-trailing-cr`; `sed 's/\[[xX]\]/[ ]/g'`; `rm -f` завжди, перевірка RC окремо; пояснення в коментарі |
| 2 | 584 | `git fetch origin --prune '+refs/heads/task/*:…'` | Refspec у команді ЗАМІНЮЄ config → `origin/main` не оновлюється; G0 читає застарілий стан (відтворено) | **Виправлено.** Повний refspec `'+refs/heads/*:refs/remotes/origin/*'` у §0, G0, §5.3, §5.5 + коментар |
| 3 | 831 | «вихід із BLOCKED … ❌ ✅» vs 834 «ABANDONED власної ✅ ✅» vs §10 | Власник-worker може вивести з BLOCKED у ABANDONED | **Виправлено.** 6.10: «ABANDONED власної одиниці з CLAIMED або ACTIVE (не з BLOCKED)»; §10: `BLOCKED → ABANDONED лише master` |
| 4 | 27 | `grep -rn '\- \[ \]' specs/` | Заморожені копії в handoffs/ містять `[ ]` назавжди → §0 показує завершені одиниці як відкриті | **Виправлено.** `--exclude-dir=handoffs` + коментар |
| 5 | 447 | «(`OWNER`, `STATE=CLAIMED`, попередній власник у `NOTE`)» | Поля, дописані після `---`, читаються G7 як критерії → RECLAIM/G9 роблять одиницю неінтегрованою (відтворено) | **Виправлено.** §10: жорстка структура, нові поля — перед `---`; §5.4 і коментар §5.2 |
| 6 (нотатка) | 314 | `git worktree add --detach ../wt-TASK-001 origin/main` | Сусідні клони двох агентів → однакова тека `../wt-<ID>`; §5.2 падає з `exit 128` без виходу | **Виправлено.** Guard перед `worktree add` (СТОП, чуже не видаляти) + правило в §5.5: клони різних агентів — у різних батьківських теках |

# Що рев'юер підтвердив як коректне (iter5)

Арбітраж claim; `AGENT_ID` unset; guard `HEAD != origin/main`; §5.5 усі три сценарії
(той самий клон із пробілом у шляху, свіжий клон, після RECLAIM → СТОП); ранній абор →
очистка → повторний claim; §5.6; `small/*` і rebase у G7-lite; §10 ↔ 6.10 (крім #3);
G8 ↔ §10 ↔ §9 ↔ §5.4; §6 intro ↔ ворота; §2.1/§2.2 ↔ 6.10 ↔ §8.2; усі ключі §12;
§14 13/13; проєктна незалежність.

# Залишкова невизначеність

- Локальні bare-репо, Git for Windows 2.52, bash; хостинги не перевірялись.
- `core.autocrlf=false` у клонах рев'юера; за замовчуванням Windows можливий CRLF у
  копії — закрито `--strip-trailing-cr`, але наживо з autocrlf=true не перевірено.
