# Спека: Безпека та розподіленість (Фаза 5, backlog)

Записано за прямим проханням користувача (2026-07-13) як пріоритетний список того, чого бракує Spirit для одночасної анонімності, безпеки й зручності. Це **backlog**, не активна робота — жодна секція не почата. Призначено для підбирання будь-яким волонтерським Claude-акаунтом у моделі "Claude Country" (кілька незалежних Claude-акаунтів працюють над однією кодовою базою на GitHub у межах своїх кредитів, кожен читає стан з репозиторію й підхоплює будь-яку неторкнуту секцію).

**Перед стартом будь-якої секції**: прочитати `CLAUDE.md` (корінь репо) для дисципліни spec-first/test-first/exec-review/commit-gate/push-gate, і `docs/decisions.md` для наявних архітектурних рішень, які секція не повинна порушувати (zero-database, D1-D12).

## Контекст: чому саме ці три пріоритети

Поточна модель Spirit: E2EE-контент (ECDH+AES-GCM) захищений добре, але (а) метадані з'єднання бачить сигнальний вузол і пряму IP бачить співрозмовник через WebRTC, (б) сесійний ключ не оновлюється post-compromise, (в) P2P працює лише поки обидві вкладки відкриті — немає способу отримати повідомлення, якщо вкладка закрита, без побудови сервера зберігання (що суперечить zero-database, D1). Секції нижче впорядковані за впливом на реальну модель загроз, не за складністю реалізації.

## Секція P1: Метадані-стійкий транспорт

- [ ] **Tests**: TBD секцією, що почне роботу — типово: `client/tests/*.test.js` для нової логіки вибору TURN/relay-режиму (форсований relay vs. p2p-first), конфіг-парсинг для власного STUN-URL.
- [ ] **Impl**: (а) UI-перемикач "форсувати TURN-релей" на екрані «Сервер» (rtcConfig `iceTransportPolicy: 'relay'`) — приховує реальну IP від співрозмовника ціною затримки/потреби в реальному TURN-сервері (Google STUN не є TURN, TURN потребує окремого хостингу з полосою пропускання — дослідити безкоштовні/дешеві опції або власний coturn на kolomedi/kibr); (б) замінити дефолтний `stun:stun.l.google.com:19302` на власний STUN, якщо можливо на існуючій інфраструктурі; (в) дослідити opційний Tor-транспорт для сигналінгу (складніше — потребує WebSocket-based signaling over onion чи snowflake-подібний bridge, це вже суттєва зміна протоколу, не просто UI-перемикач — почати з дослідницької нотатки в `docs/`, не з коду).
- [ ] **Exec review**: —

## Секція P2: Ratchet поверх наявного ECDH-хендшейку

- [ ] **Tests**: `client/tests/e2ee.test.js` (доповнення чи новий `ratchet.test.js`) — новий сесійний ключ виводиться після N повідомлень чи по таймеру, старий ключ більше не відновлюваний (forward secrecy в межах сесії), сумісність з наявним `deriveSessionKey`/`encryptMessage`/`decryptMessage` API.
- [ ] **Impl**: `client/js/e2ee.js` чи новий `client/js/ratchet.js` — почати з дослідження мінімального симетричного ratchet (KDF-chain на базі HKDF, без повного Double Ratchet зі скачками gaps/skipped-message handling, якщо це надмірна складність для MVP) поверх наявного per-сесійного `sessionKey`. Не змінювати сам ECDH-хендшейк (Секції 1-5, `specs/phase1/mvp.md`) без окремого обговорення — лише шар над ним.
- [ ] **Exec review**: —

## Секція P3: Encrypted push notifications

- [ ] **Tests**: TBD — залежить від обраного підходу (Web Push API потребує підписки на push-сервіс браузера — Google/Mozilla-подібний, що вже частковий відступ від "нуль сторонніх сервісів"; дослідити чи це прийнятний компроміс, обговорити з користувачем ПЕРЕД імплементацією, оскільки це змінює модель довіри).
- [ ] **Impl**: TBD.
- [ ] **Exec review**: —

## Секція P4 (нижчий пріоритет): дрібніші пункти з того самого обговорення

- Sybil-стійкість: розподілена ботнет-ферма з багатьох IP обходить поточний per-IP rate-limiting — потребує дослідження (proof-of-work на create_invite? capped за замовчуванням суворіше?).
- UI-підказка "звірте safety number" при першій зустрічі з новим контактом (safety number вже реалізований технічно, але прихований — ніхто не використовує).
- Соціальне відновлення портативного акаунта (Shamir secret sharing між довіреними контактами) — компенсує незворотність втрати пароля в Argon2id-схемі (Фаза 3).

## Верифікація

Кожна секція — окремий spec-first/test-first цикл per дисципліну `CLAUDE.md`. Секція P1(в) і Секція P3 explicitly вимагають попереднього обговорення з користувачем ПЕРЕД кодом (зміна моделі довіри/архітектури), не лише test-first — не пропускати AskUserQuestion-крок навіть якщо технічно "очевидно, що робити".
