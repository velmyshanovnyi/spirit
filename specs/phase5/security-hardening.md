# Спека: Безпека та розподіленість (Фаза 5, backlog)

Записано за прямим проханням користувача (2026-07-13) як пріоритетний список того, чого бракує Spirit для одночасної анонімності, безпеки й зручності. Це **backlog**, не активна робота — жодна секція не почата. Призначено для підбирання будь-яким волонтерським Claude-акаунтом у моделі "Claude Country" (кілька незалежних Claude-акаунтів працюють над однією кодовою базою на GitHub у межах своїх кредитів, кожен читає стан з репозиторію й підхоплює будь-яку неторкнуту секцію).

**Перед стартом будь-якої секції**: прочитати `CLAUDE.md` (корінь репо) для дисципліни spec-first/test-first/exec-review/commit-gate/push-gate, і `docs/decisions.md` для наявних архітектурних рішень, які секція не повинна порушувати (zero-database, D1-D12).

## Контекст: чому саме ці три пріоритети

Поточна модель Spirit: E2EE-контент (ECDH+AES-GCM) захищений добре, але (а) метадані з'єднання бачить сигнальний вузол і пряму IP бачить співрозмовник через WebRTC, (б) сесійний ключ не оновлюється post-compromise, (в) P2P працює лише поки обидві вкладки відкриті — немає способу отримати повідомлення, якщо вкладка закрита, без побудови сервера зберігання (що суперечить zero-database, D1). Секції нижче впорядковані за впливом на реальну модель загроз, не за складністю реалізації.

## Секція P1: Метадані-стійкий транспорт

- [x] **Tests**: (а) `client/tests/rtcConfig.test.js` (новий, 3 тести — `buildRtcConfig`: дефолт без `iceTransportPolicy`, явний `forceTurnRelay:false` теж без ключа, `forceTurnRelay:true` → `"relay"`) + `client/tests/app.test.js` (доповнення, 3 тести — чекбокс на екрані «Сервер» коректно потрапляє в `rtcConfig` на ВСІХ шляхах побудови з'єднання: ініціатор і джойнер). (б)/(в) — не потребували коду, закриті дослідженням (див. Impl нижче).
- [x] **Impl**: (а) **реалізовано кодом** — UI-чекбокс `#force-turn-relay` ("Форсувати TURN-релей") на екрані «Сервер» поруч із `stun-url`; чиста функція `buildRtcConfig(stunUrl, {forceTurnRelay})` у `client/js/webrtc.js` додає `iceTransportPolicy: 'relay'` лише коли увімкнено (за замовчуванням ключ відсутній повністю, не `'all'`); підключено у ВСІХ 5 місцях побудови `rtcConfig` у `client/js/app.js` (ініціатор чату, приєднання, обидва боки device-linking, invite-link auto-join) — не лише в одному, щоб forced-relay peer не намагався з'єднатись без relay на одній зі сторін. i18n-підказка (`server.forceTurnRelayHint`, усі 11 локалей) explicitly попереджає, що звичайний публічний STUN (Google) не є TURN і з'єднання просто не встановиться без справжнього TURN URL (з credentials) у тому самому полі `stun-url`. Обмеження, свідомо залишене мінімальним (варіант (а) з опису задачі): `iceServers` підтримує лише одне поле `urls` без окремих `username`/`credential` — сумісно з наявним форматом поля `stun-url`, користувач має вписати туди TURN URL, що вже містить/приймає ауth за потреби провайдера, чи використати провайдера з URL-based доступом; окремих полів для TURN username/password НЕ додано цією секцією (scope мінімізовано за рекомендацією задачі). (б) **не потребувало коду** — дослідження (`docs/metadata-resistant-transport.md`) встановило, що власний STUN/TURN технічно неможливий на поточному shared-хостингу kolomedi/kibr (немає SSH/довготривалих процесів/контролю UDP-портів); практичний шлях — сторонній TURN-сервіс (Metered.ca тощо) через те саме поле `stun-url`, без додаткового коду. (в) **не потребувало коду** — дослідження підтвердило, що `client/js/signalingClient.js`'s `apiRequest` вже приймає будь-який URL, включно з `.onion`, без protocol-фільтра; Tor Browser-користувачі вже сьогодні можуть вписати `.onion`-адресу сигнального вузла в наявне поле «Сигнальний вузол» без змін клієнта.
- [x] **Exec review**: iter1 — [reviews/security-hardening-P1a-iter1.md](../reviews/security-hardening-P1a-iter1.md). Нуль знахідок; підтверджено коректну відсутність `iceTransportPolicy` за замовчуванням, консистентне підключення на всіх 5 точках побудови `rtcConfig`, відсутність нової інфраструктури персистентності, якість тестів (не vacuous), відповідність i18n-ключів. Одне непов'язане з дефектом зауваження (не потребує фіксу): forced-relay з лише STUN-url тихо зависає на ICE-gathering замість явної помилки — пом'якшено підказкою в UI, активне виявлення цього стану залишено поза обсягом секції.

## Секція P2a: Ratchet — чисте крипто-ядро (без інтеграції в чат)

Той самий підхід, що вже виправдав себе для Argon2id-схеми (Фаза 3, Секції H1-H2): спочатку самодостатній, повністю протестований крипто-модуль, окремий exec review, і лише ПОТІМ (Секція P2b) — інтеграція в реальний потік повідомлень `app.js`/`e2ee.js`.

Мінімальний симетричний ratchet (KDF-chain на базі HKDF, БЕЗ повного Double Ratchet — без DH-ratchet-кроку, без skipped-message/out-of-order handling). Дає forward secrecy в межах сесії (компрометація поточного chain-key не розкриває попередні message-keys), НЕ дає повного post-compromise security (немає нового DH-обміну — це відкладено як можливе розширення, якщо P2a-P2b виявляться недостатніми).

- [x] **Tests**: `client/tests/ratchet.test.js` (новий файл) — `deriveRootKey(privateKey, publicKey)` (ECDH+HKDF, домен-розділений від `e2ee.js`'s `deriveSessionKey` тим самим shared secret — тобто криптографічно незалежний, той самий safety-перевірений patтерн, що й у `deterministicIdentity.js`); `deriveInitialChainKeys(rootKeyBytes, localEcdhWire, peerEcdhWire)` повертає `{sendChainKey, receiveChainKey}`, симетрично узгоджені для обох сторін (лексикографічне сортування wire-рядків визначає, яка сторона на якому "chain" шле/приймає — обидві сторони приходять до ОДНАКОВОГО розподілу); `ratchetStep(chainKeyBytes)` повертає `{messageKey, nextChainKeyBytes}` — `messageKey` не дозволяє відновити `chainKeyBytes` (forward secrecy), послідовні виклики дають різні `messageKey`, ланцюжок детермінований (той самий `chainKeyBytes` завжди дає той самий наступний крок). 9/9 тестів зелені.
- [x] **Impl**: `client/js/ratchet.js` (новий, чиста крипто-логіка, без залежності від `app.js`/`e2ee.js`).
- [x] **Exec review**: iter1 — [reviews/security-hardening-P2a-iter1.md](../reviews/security-hardening-P2a-iter1.md). Нуль знахідок, домен-розділення від `e2ee.js` підтверджено, forward secrecy та детермінізм ланцюжка перевірені.

## Секція P2b: Інтеграція ratchet у потік повідомлень чату

- [x] **Tests**: `client/tests/app.test.js` (доповнення) — кожне надіслане/отримане ЧАТ-повідомлення (лише user-typed текст, `btn-send`) використовує НОВИЙ `messageKey` замість статичного `sessionKey`; ланцюжки send/receive синхронізовані між `startInitiatorSession`/`startJoinerSession`; конкурентні вхідні R1-повідомлення серіалізуються, не десинхронізують ланцюжок (exec-review iter1 знахідка); R1-повідомлення в вікні "sessionKey вже є, ланцюжок ще ні" тихо відкидається (exec-review iter1 знахідка). Рішення щодо control-типів (identity-announce, device-list-announce, proof-set-announce, webrtc-call-offer/answer, device-linking grant/request): **лишаються на статичному `sessionKey`, без змін** — звужено після виявлення, що повний ratchet торкнувся б ~40 наявних тестових асертів у `app.test.js` (узгоджено з користувачем, опційне розширення на "все на ratchet" залишено в backlog за потреби). 142/142 тестів у `app.test.js` зелені.
- [x] **Impl**: `client/js/app.js` — wire-маркер `"R1:"` розрізняє ratchet-зашифровані чат-повідомлення від control-повідомлень у спільній точці розшифрування (`onMessage`); `state.sendChainKey`/`state.receiveChainKey` деривуються поруч із `state.sessionKey` в обох сесійних хендшейках; `serializedChainStep`-локи (`state.sendChainLock`/`state.receiveChainLock`) серіалізують доступ до ланцюжків, запобігаючи гонці при конкурентних повідомленнях.
- [x] **Exec review**: iter1 — [reviews/security-hardening-P2b-iter1.md](../reviews/security-hardening-P2b-iter1.md) (2 знахідки: неатомарне просування ланцюжка під конкурентними повідомленнями; guard `sessionKey` не покриває готовність ланцюжка). Обидві виправлено. iter2 — [reviews/security-hardening-P2b-iter2.md](../reviews/security-hardening-P2b-iter2.md), збіжність, нуль нових знахідок.

## Секція P3: Encrypted push notifications

Рішення узгоджені з користувачем 2026-07-18 (AskUserQuestion) — Web Push API прийнятний компроміс, підписка живе виключно P2P між контактами (сервер її не бачить/не зберігає), лише для збережених верифікованих контактів. Повний дизайн, VAPID-модель і розбивка на секції PN1-PN5 — окремий файл [specs/phase5/push-notifications.md](push-notifications.md), щоб не роздувати цю спеку. Ключовий архітектурний наслідок: zero-database (D1) лишається непорушним — push доставляє НЕ повідомлення, а лише "дзвінок у двері" (invite-посилання одразу відкриває наявний zero-click auto-join, Секція F4), оскільки справжня доставка пропущених повідомлень без серверного сховища технічно неможлива.

- [x] **Tests**: див. [push-notifications.md](push-notifications.md) Секції PN1-PN5 (усі 5 завершені, 521/521 по проєкту).
- [x] **Impl**: див. [push-notifications.md](push-notifications.md) Секції PN1-PN5.
- [x] **Exec review**: усі 5 секцій зійшлись (PN1/PN2/PN4/PN5 — iter1, PN3 — iter2), знахідки задокументовані й виправлені в [reviews/](../reviews/).

## Секція P4 (нижчий пріоритет): дрібніші пункти з того самого обговорення

- Sybil-стійкість: розподілена ботнет-ферма з багатьох IP обходить поточний per-IP rate-limiting — потребує дослідження (proof-of-work на create_invite? capped за замовчуванням суворіше?).
- ~~Соціальне відновлення портативного акаунта (Shamir secret sharing між довіреними контактами) — компенсує незворотність втрати пароля в Argon2id-схемі (Фаза 3).~~ ЗАВЕРШЕНО: повний дизайн і розбивка на секції S1-S3 — окремий файл [specs/phase5/social-recovery.md](social-recovery.md), щоб не роздувати цю спеку. S1 (чисте Shamir-ядро над GF(256)), S2 (розподіл часток довіреним контактам, P2P-announce + текстовий експорт) і S3 (флоу відновлення — перегляд утримуваних часток довіреною особою, комбінування часток і адопція ідентичності власником) усі завершені й review-пройдені.

### P4: UI-підказка safety number (завершено 2026-07-18)

Safety number вже реалізований технічно (fingerprint peer'а показувався в `connection-status`), але лише як миттєвий status-рядок, що перезаписується наступними подіями — ніхто не встигав його звірити. Тепер — окремий, персистентний елемент `#safety-number-hint` на екрані розмови, що лишається видимим (не автозникає) з першої верифікації нового контакту, аж доки той самий контакт не повернеться вже відомим, чи не станеться logout/нова сесія.

- [x] **Tests**: `client/tests/app.test.js` (5 нових тестів, "Section P4") — хінт показується з fingerprint-ом при першій зустрічі з НОВИМ контактом (режим профілю); НЕ показується для вже ВІДОМОГО контакту; показується в ефемерному режимі (там персистентності нема, тож кожна зустріч — фактично перша); реально СКИДАЄТЬСЯ, коли той самий peer повторно announce'ується вже відомим (exec-review iter1 знахідка — тест ловить саме перехід true→false, а не лише "почав прихованим"); не переживає logout (exec-review iter1 знахідка). 532/532 по проєкту (один `codec.test.js`-збій у повному прогоні — відомий flaky тест на великих буферах, підтверджено 4/4 в ізоляції).
- [x] **Impl**: `client/js/app.js` (гілка `identity-announce` в `handleChatMessage` рахує `isFirstMeeting`, `hideSafetyNumberHint()`-хелпер викликається на всіх 4 точках скидання `state.peerFingerprint`), `client/index.html` + `client/tests/app.test.js` fixture (`#safety-number-hint`, клас `.banner-warn`, без нового CSS-токена), `client/js/i18n.js` (`safety.hint` у всіх 11 локалях).
- [x] **Exec review**: iter1 — [reviews/security-hardening-P4-safety-number-hint-iter1.md](../reviews/security-hardening-P4-safety-number-hint-iter1.md). 2 знахідки (застарілий хінт від попереднього peer'а переживав logout/нову сесію; тест на "відомий контакт" не перевіряв реальне скидання) — обидві виправлено, збіжність на iter2 (задокументовано в тому самому файлі).
- Per-frame ratchet для аудіо/відео (Insertable Streams) — НЕ заплановано найближчим часом. Питання від користувача 2026-07-17 "щоб і для відео та аудіо потоків теж працювало" з'ясовано: аудіо/відео вже наскрізно зашифровані обов'язковим WebRTC DTLS-SRTP (незалежно від довіри до сигнального/TURN-вузла — див. розділ "Аудіо/відео (дзвінки)" у [docs/e2ee.md](../../docs/e2ee.md)); ratchet із Секції P2b свідомо покриває лише текстовий чат, не медіа. Розширення на per-frame ratchet для медіа — окрема секція, що вимагає explicit обговорення (крос-браузерна підтримка Insertable Streams нерівномірна, конфліктує з інваріантом кросплатформності).

## Верифікація

Кожна секція — окремий spec-first/test-first цикл per дисципліну `CLAUDE.md`. Секція P1(в) і Секція P3 explicitly вимагають попереднього обговорення з користувачем ПЕРЕД кодом (зміна моделі довіри/архітектури), не лише test-first — не пропускати AskUserQuestion-крок навіть якщо технічно "очевидно, що робити".
