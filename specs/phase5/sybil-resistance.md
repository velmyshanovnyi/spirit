# Спека: Sybil-стійкість через proof-of-work на create_invite (Секція P4)

Розширення бектрогу `specs/phase5/security-hardening.md`'s Секції P4 ("Sybil-стійкість"). Рішення узгоджене з користувачем 2026-07-18 через AskUserQuestion: **proof-of-work на `create_invite`**, а не лише суворіші дефолтні ліміти (які не вирішують саму Sybil-проблему — розподілена ботнет-ферма з багатьох IP все одно обходить per-IP `RateLimiter.php`, кожен IP отримує свіжу квоту).

## Контекст

`server/library/RateLimiter.php` вже реалізує per-IP ковзне вікно (`checkAndRecordRoomCreation`, дефолт `M=10` room-creations/год на IP). Це best-effort захист від одного зловмисника з обмеженою кількістю IP, але безсилий проти справжньої ботнет-ферми: N різних IP = N незалежних квот. `create_invite` — єдина дія, що фактично створює нову кімнату (`create_offer` вимагає вже існуючого `room_id` з `create_invite`), тож PoW саме на `create_invite` перекриває весь потік створення спаму-кімнат.

## Дизайн: stateless hashcash-подібний PoW, прив'язаний до часового вікна

Без зайвого round-trip (клієнт не запитує "виклик" окремим запитом — сумісно з філософією "signaling — один POST-ендпоінт", `docs/signaling-protocol.md`) і без порушення zero-database (D1): challenge детермінований з поточного часу, квантованого у вікна, а не з серверного секрету чи збереженого стану.

1. **Часове вікно**: `timeWindow = floor(unixTime / POW_WINDOW_SECONDS)`, рекомендовано `POW_WINDOW_SECONDS = 30`.
2. **Challenge-рядок**: `${timeWindow}:${sender_key}` — прив'язка до `sender_key` означає, що кожна "нова особа" (свіжий identity-ключ, як і генерує кожен реальний або симульований Sybil-акаунт) повинна розв'язати ВЛАСНИЙ PoW; той самий nonce не підходить для іншого `sender_key`.
3. **Розв'язок (клієнт)**: знайти `nonce` (випадковий рядок/число), для якого `SHA-256(challenge + ":" + nonce)` має щонайменше `POW_DIFFICULTY_BITS` провідних нульових бітів. **Перекалібровано з 20 на 16 біт 2026-07-18** за результатами живого вимірювання (див. "Пост-деплой знахідка" нижче) — на 20 бітах реальний клік у браузері зависав на 30+ секунд; на 16 (~2^16 ≈ 65K хешів) — стабільно ~1-1.5с.
4. **Запит**: `create_invite` додає до тіла `{ "pow_timestamp": <unix-секунди, той самий, що дав timeWindow>, "pow_nonce": "<рядок>" }`.
5. **Перевірка (сервер)**:
   - Recompute `timeWindow` з `pow_timestamp`; перевірити `|serverNow - pow_timestamp| <= 2 * POW_WINDOW_SECONDS` (толерантність до дрейфу годинника й межі вікна) — інакше `400`.
   - Recompute хеш, перевірити складність (провідні нульові біти) — інакше `400`.
   - **Anti-replay**: перевірити, що трійка `(timeWindow, sender_key, nonce)` ще не використовувалась — файловий лічильник `pow_spent.json`, той самий патерн блокування/TTL, що й `RateLimiter.php`/`Storage.php` (запис живе лише поки не спливе `2 * POW_WINDOW_SECONDS`, потім природний GC). Без цього кроку розв'язаний PoW можна один раз обчислити і "відтворити" з тисяч різних IP протягом того самого вікна — саме той Sybil-обхід, що PoW має закривати. Той самий клас best-effort застереження, що й `RateLimiter.php` (можливі гонки на shared-хостингу з кількома PHP-воркерами без спільної пам'яті — прийнятно, не гірше за наявний rate-limiter).
   - Невдала перевірка PoW → `400 Bad Request` з чітким кодом помилки (не `429` — це не rate-limit, а невалідний/відсутній доказ роботи), НЕ рахується в per-IP rate-limit лічильник як "успішний" запит (щоб не давати додаткового способу вичерпати чужу квоту підробленими PoW-запитами — але сам факт запиту все одно проходить через `checkAndRecordRequest`, загальний ліміл, per `RateLimiter.php`'s "records regardless of outcome" філософію).

## Секція SR1: PoW крипто-ядро (клієнт + сервер, чисті функції)

- [x] **Tests**: `client/tests/pow.test.js` (новий) — `solvePow(challenge, difficultyBits)` знаходить nonce, для якого перевірка проходить; `verifyPow(challenge, nonce, difficultyBits)` (той самий алгоритм, що і на сервері, для симетричного клієнтського самотесту й для юніт-тестів) коректно рахує провідні нульові біти на кількох відомих хеш-префіксах; заниження складності явно провалює перевірку. `server/verify/section_pow.php` (новий, live-verification harness за патерном `section9_rate_limiter.php`) — PHP-версія `verifyPow` дає ІДЕНТИЧНИЙ результат до JS-версії на тих самих тестових векторах (крос-мовна відповідність — найбільший ризик підступного бага: якщо PHP і JS по-різному рахують провідні нульові біти чи по-різному кодують рядок у байти перед SHA-256, легітимні клієнти будуть відхилятись). 596/596 клієнтських тестів зелені (590 базових + 6 нових).
- [x] **Impl**: `client/js/pow.js` (`solvePow`, `verifyPow`, `buildPowChallenge(timeWindow, senderKey)` — чисті функції на Web Crypto `crypto.subtle.digest("SHA-256", ...)`), `server/library/Pow.php` (`verify(challengeString, nonce, difficultyBits): bool`, той самий алгоритм). `isNonceSpent`/anti-replay навмисно НЕ реалізовано в SR1 — це частина SR2 (інтеграція, з файловим лічильником `pow_spent.json`).
- [x] **Exec review**: Opus subagent, зійшовся на 1-й ітерації, 0 блокуючих знахідок; крос-мовна відповідність (JS/PHP підрахунок провідних нульових бітів) перевірена по всіх edge-кейсах + незалежно перерахованими тестовими векторами. Артефакт: `specs/reviews/sybil-resistance-SR1-iter1.md`.

## Секція SR2: Інтеграція в `create_invite` (сервер) + автоматичне розв'язання (клієнт)

- [x] **Tests**: `server/verify/section_pow_integration.php` — `create_invite` без PoW-полів чи з невалідним PoW → `400`; з валідним PoW → `success`; повторне використання тієї самої `(timeWindow, sender_key, nonce)` трійки протягом вікна → `400` (anti-replay); застарілий `pow_timestamp` (поза толерантністю) → `400`; різний `sender_key` з власним валідним розв'язком того самого вікна успішний незалежно; `PowNonceStore` GC/TTL-поведінка з коротким вікном. `client/tests/pow.test.js` — 2 нових тести (regression-фікс з exec-review iter1: `solvePow` рандомізує старт пошуку, тож два виклики з однаковим challenge дають РІЗні nonce, обидва незалежно валідні; явний `startAttempt` лишається детермінованим для тестів). `client/tests/signalingClient.test.js` — `createInvite()` розв'язує PoW ПЕРЕД відправкою запиту (мокає `pow.js`), включає `pow_timestamp`/`pow_nonce` в тіло запиту, викликає `onPowStart` до розв'язання. `client/tests/app.test.js` — статус "Розв'язання захисту від спаму..." показується під час розв'язання PoW у `initiateChatSession()`. 601/601 по проєкту (596 базових + 3 signalingClient + 2 pow.js regression-фікс).
- [x] **Impl**: `server/library/PowNonceStore.php` (новий, anti-replay файловий лічильник, той самий патерн, що й `RateLimiter.php`), `server/library/Pow.php` (додано `buildChallenge(timeWindow, senderKey)`), `server/library/SignalingController.php` (`create_invite`-гілка викликає `checkPow()` ДО створення кімнати — перевірка полів, свіжість timestamp, `Pow::verify`, anti-replay), `server/config.php` (`POW_SPENT_FILE`, `POW_WINDOW_SECONDS=30`, `POW_DIFFICULTY_BITS=16` — перекалібровано з 20, див. нижче), `server/public/index.php` (require нових файлів), `client/js/signalingClient.js`'s `createInvite()` (розв'язує PoW перед POST, додає `pow_timestamp`/`pow_nonce`, `onPowStart`-колбек, `POW_DIFFICULTY_BITS=16`), `client/js/pow.js` (`solvePow` рандомізує старт пошуку — exec-review iter1 фікс; конкурентні батчі замість послідовного `await` на кожен nonce — пост-деплой фікс, див. нижче), `client/js/app.js` (обидва виклики `createInvite()` показують статус розв'язання), `client/js/i18n.js` (`status.solvingPow` у всіх 11 локалях).
- [x] **Exec review**: iter1 — [reviews/sybil-resistance-SR2-iter1.md](../reviews/sybil-resistance-SR2-iter1.md), 1 блокуюча знахідка (детермінований `solvePow` конфліктував з per-identity anti-replay: та сама особа, що створює 2 інвайти в тому самому 30с вікні, отримувала однаковий nonce і другий запит відхилявся як "replay") — виправлено рандомізацією старту пошуку в `solvePow`. iter2 — [reviews/sybil-resistance-SR2-iter2.md](../reviews/sybil-resistance-SR2-iter2.md), збіжність, 0 блокуючих знахідок.

### Пост-деплой знахідки (не виявлені тестами/review, знайдені живою перевіркою реального кліку в браузері)

Той самий урок, що й з Service Worker у PN3: unit-тести (Node/vitest) і exec-review не ловлять продуктивність реального `crypto.subtle` у браузері — лише жива перевірка справжнього UI-кліку це показала.

1. **`solvePow` був катастрофічно повільним у реальному браузері**. Оригінальна реалізація робила `for (...) { await verifyPow(...) }` — по одному `crypto.subtle.digest` виклику ЗА РАЗ, послідовно. Кожен `await` має реальні накладні витрати диспетчеризації в реальному браузері; на рекомендованій складності 20 біт (~2^20 ≈ 1M очікуваних спроб) реальний клік "Швидкий чат" зависав на 30+ секунд замість очікуваної <1с. У Node/vitest цей ефект НЕ відтворюється (там `crypto.subtle` не має такого асинхронного оверхеду) — тест продуктивності, написаний для цього фіксу, проходить в Node незалежно від того, чи є баг, тож він лише регресійна підстраховка, не доказ коректності в браузері. Виправлено: `solvePow` тепер диспетчерує кандидатів конкурентними батчами (`Promise.all`, `DEFAULT_BATCH_SIZE=256`) замість повністю послідовного пошуку — поведінка (детермінізм при явному `startAttempt`, порядок повернення першого знайденого nonce) незмінна, змінюється лише wall-clock час.
2. **Навіть після батчингу 20 біт лишились нереалістично повільними в тому самому браузерному середовищі** (30+с) — живе вимірювання показало: 10 біт ~61мс, 12 біт ~51мс, 14 біт ~1264мс, 16 біт ~1124мс (висока дисперсія, порядок секунд навіть на малій складності — оверхед на виклик, а не сам хеш, домінує). **`POW_DIFFICULTY_BITS` перекалібровано з 20 на 16** на обох сторонах (клієнт+сервер) — 16x менша вартість для атакуючого, ніж планувалось, але реальний клік "Швидкий чат" тепер стабільно завершується за ~1-1.5с замість зависання.
3. Живо підтверджено ПІСЛЯ обох фіксів на `spirit.kibr.com.ua` (свіжий import з cache-bust, обхід кешування статичних файлів у Browser pane sandbox — окрема, вже задокументована в agent-звітах, особливість тестового середовища, не дефект застосунку): `solvePow` на 16 бітах послідовно <1.5с, `create_invite` успішно створює кімнату.

## Верифікація

SR1 (чисте крипто-ядро, клієнт+сервер симетрично) — безпечно почати без додаткових питань, той самий паттерн crypto-core-first. SR2 (реальна інтеграція в живий протокол) потребує live-перевірки на обох тестових хостах (реальний `create_invite` виклик через живий PHP-вузол, не лише PHPUnit-подібний verify-harness) — PHP-код виконується на сервері, а не в Browser pane sandbox, тож перевірка = реальний HTTP-запит з клієнта до задеплоєного `server/public/index.php` на kolomedi/kibr.

**Явний масштаб цієї секції**: PoW додає тертя лише на `create_invite` (створення НОВОЇ кімнати). Він НЕ захищає `create_offer`/`get_offer`/`submit_answer`/`check_answer` — вони і так вимагають вже існуючого валідного `room_id`+`invite_token`, здобутого через `create_invite`, тож Sybil-вигода від їх флуду без валідного інвайту відсутня (per-IP `RateLimiter.php`'s загальний ліміл лишається єдиним захистом там, як і зараз).
