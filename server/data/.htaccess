# Deny all direct HTTP access -- database.json/ratelimit.json/signaling.lock
# must only ever be read/written by server-side PHP, never served raw.
<IfModule mod_authz_core.c>
Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
Order allow,deny
Deny from all
</IfModule>
